node_modules
dist
dist-demo
dev-dist
coverage
*.local
.env
.DS_Store
test-results
playwright-report
bench/*.y4m
bench/*.quick.md
bench/*.quick.json

# StrykerJS (§13.5 mutation gate)
.stryker-tmp
reports
stryker-setup-*.js

# The Pages artifact the service-worker guard is built into (playwright.config.ts).
# `docs/` is the committed one; this is a scratch copy of the same build.
dist-pages
