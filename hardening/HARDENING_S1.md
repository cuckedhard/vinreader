# HARDENING_S1 — ledger

The §13 hardening loop against slice S1 (camera scanning). Rows follow §13.3.

**Severity** (§13.3): **S1** blocker — data loss, wrong VIN accepted, crash, N-rule violation · **S2** major — spec deviation, missing error state, offline break · **S3** minor — UX, perf, microcopy · **S4** nit.

> **Statuses reconciled 2026-09-04** against the code and the history, row by row, not by grepping commit messages. 56 rows were corrected: they said `open` while the work had landed, mostly because round 1's fixes went in under commits that named no finding id. Two rows said something worse than nothing — B4 asserted a measurement its own tracked evidence refutes, and A01's repro column had inverted in meaning when its tests were flipped. A ledger that misreports is the same defect as a guard that cannot fail: a record that does not record. Rows still marked `open` below are genuinely open.

**Buckets**: **FIX** in scope for this loop · **NEEDS-ZACH** touches a §4 constant, an N-rule, or slice scope, and is never applied by an agent · **WONTFIX** with a stated reason.

**Budget**: `R_MAX = 6` rounds. The loop stops at §13.6 or the budget, whichever comes first, and never continues into another slice.

## Scope

S1 is the camera scanner: `useScanner.ts`, `scanMachine.ts`, `CameraView.tsx`, `feedback.ts`, and the §6.3 state machine, §4.6 symbologies, §4.2 extraction as the scanner uses it, and the §6.1 and §6.4 field rules that govern the scan screen. A regression found in an earlier slice is a defect and gets fixed; a new feature is not, and goes to NEEDS-ZACH.

## Rounds

| id | sev | area | spec ref | description | repro / test | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| B1 | S2 | bench | §13.4, §13.6 | The degradation tiers are **not ordered**. `moderate` applies a σ1.5 blur; `severe` applies none. Measured over 200 VINs, moderate is harder than severe on every 1D symbology: code_39 77.5% moderate vs 80.5% severe, code_39_i 79.0% vs 73.5%, code_128 81.0% vs 85.5%. §13.6's 99/90/70 ladder assumes severe ⊃ moderate, so the thresholds cannot mean what they intend. The implementation matches §13.4 exactly — the gap is in the spec's tier definitions, so no agent may change it. | `bun run bench --count 200` | NEEDS-ZACH | fixed | 38fdd31 |
| B2 | S2 | bench | §13.4 | `bench/decode.ts` was **not the app's decode path**: it decoded a PNG in node through `RGBLuminanceSource` + `MultiFormatReader.decode`, where the app runs `BrowserMultiFormatReader.decodeFromCanvas` over `HTMLCanvasElementLuminanceSource` + `decodeWithState`. Fixed by decoding in Chromium through the app's own reader, built from the app's `buildScanHints()` — `bench/browser-entry.ts` + `bench/browser-decode.ts`, one page pool, frames batched over CDP. Every frame is now degraded once and offered to each instrument, so the report carries the **delta** cell by cell. **The delta is zero.** 4,200 frames, seed `0x5eed1a7c`: `canvas` 2,839 correct, `rgb` 2,839 correct, 0 frames read only by one, and 0 frames where the decoded text differed by a byte. The false-negative claim does not reproduce: on a grey frame both luminance sources compute the same buffer (`(r+2g+b)/4` and `(306r+601g+117b+512)>>10` are both exactly `v` at `r=g=b=v`), and the two real differences — `isRotateSupported()` unlocking `OneDReader`'s 90° `TRY_HARDER` retry, which cannot help an already-horizontal symbol, and `decodeWithState` vs `decode(bitmap, hints)` — change no outcome here. So every previous round's bench numbers were right about the app by luck of the corpus being grey, and are now right by construction. See B2a for the part of the gap that is **not** zero. | `bun run bench` — the delta table in `bench/report.md`; `--paths canvas,rgb` isolates it | FIX | fixed | (uncommitted) |
| B2a | S3 | bench | §13.4, §13.7 | **A real camera frame reads slightly *worse* than the bench's canvas, and the bench cannot see it.** `bench/camera-probe.ts` pads a corpus subset onto one 1920×1080 white field, writes it as `C420` YUV4MPEG2 and drives Chromium's own `--use-file-for-fake-video-capture` — so the frame reaches `drawImage(video)` through I420 exactly as a camera's would — then decodes each frame twice, once off the video and once off the identical padded PNG. 105 frames (5 VINs × 7 symbologies × 3 tiers, same seeds): **camera 67/105, canvas 69/105, 0 false accepts either way**, and the 2 lost frames are the same 2 on every run. The cause is colour range, not optics: a `C420` stream with no `XCOLORRANGE` is expanded as studio-swing, which clips and requantises. The modelled `yuv` path in the bench reproduces the *size* of this (4 frames of 630 move, in both directions) but not the *sign*. Left open rather than fixed: the whole corpus through the camera is a 12.4 GB y4m and about three hours of playback, and padding every frame to one resolution is a degradation §13.4 does not define. §7 item 4 still owns the real thing. | `bun run bench/camera-probe.ts --count 5 --fps 4` | WONTFIX | open | — |
| B3 | S3 | bench | §13.4, §13.6 | `qr_code` severe measures 0/200. The glare band is sized as a fraction of the image **diagonal**, so it covers a far larger share of a square 2D symbol than of a wide 1D one, erasing more than ECC-M can recover. Physically arguable — a real highlight of fixed size does cover more of a smaller code — but it makes the severe threshold unreachable for 2D by construction. Compounded by B2. | `bun run bench`; sample images under the scratch dir | NEEDS-ZACH | obsolete | 38fdd31 |
| B4 | — | scanner | §13.6 crit. 4 | **SUPERSEDED — this is no longer true, and the row is kept because the reason matters.** It read "zero false accepts across 3,000 attempts, every symbology and tier". The tracked `bench/report.md` now records **1 false accept in 3,000**: `code_128` severe, `EH8U2YHX60HU8VGWD` read as `EH8U2YHX60HU7VAWD` (R4-F, open with Zach). Z5's tier change did not cause it — it **revealed** it: under the old severe tier the same VIN at the same seed decoded to `null`, destroyed and therefore invisible. A tier at 0% was not merely uninformative; it was hiding the one number §13.6 exists to protect. Original text follows: no wrong VIN was ever confirmed. All 573 misses were `no_decode`; not one was a case where the decoder read a symbol and `extractVin` mishandled it. This is the §13.6 criterion the loop exists to protect and it holds. | `bun run bench --count 200` | — | pass | — |

### Round 1 — audits (spec-auditor, field-auditor, adversary, test-author)

36 raw findings, deduplicated to 29. Five were reported independently by two or three auditors, which is the strongest signal in the set.

| id | sev | area | spec ref | description | repro / test | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| A01 | S1 | scan/extraction | §4.2 step 4a (and step 1), §6.3 two-read | **§4.2 step 4a returns a straddling window instead of the VIN when one legal character precedes it — a false accept marked check-digit-valid**. §4.2 step 1 strips whitespace BEFORE step 2 splits into runs, so a multi-field label or 2D payload becomes one run; step 4a then ranks a window "aligned to a run's start" above every later window. When one or more §4.1-legal characters sit immediately in front of the VIN, the offset-0 window straddles the boundary — and it passes the check digit whenever position 9 of the straddle happens to be a  | `bun run test -- src/lib/vin/extractVin.adversary.test.ts` — **read this column with care: it has inverted.** It used to say the tests passed because they *characterised* the hazard. They were flipped at Z6 and again at R4-A, so today they assert the REFUSAL: a green run now means the defect is closed, not that it is reproduced. | NEEDS-ZACH | fixed | 9dc8d36 |
| A02 | S2 | scanner | §6.3 ("Cooldown: the same VIN confirmed  | **The §6.3 cooldown cannot work: the machine (and its cooldown map) is destroyed by the navigation that ends every successful scan** (found independently by 3: adversary,spec,tests). The cooldown lives in `useReducer` state inside `useScanner` (useScanner.ts:132), i.e. in ScanScreen's component instance. Every successful commit navigates to the Sheet (useVinCommit.ts:78, called from ScanScreen.tsx:66), and `/scan` and `/v/:vin` are sibling routes (src/app/router.tsx:16-17), so ScanScreen unmounts and the whole machine — cooldown map included — is thrown away. `accept(read.vin) | e2e, extending tests/e2e/camera.spec.ts: after `await expect(page).toHaveURL(/#\/v\/1HGCM82633A004352/)`, click the "Scan" nav link and wait ~5 s with | FIX | fixed | af16a08 |
| A03 | S2 | scanner | §6.3 ("`stream_lost` (track ended, tab h | **A tab hidden longer than 30 s returns to idle but never re-requests the camera on that visibility** (found independently by 2: field,spec). In the `visible` branch the reducer computes the hidden gap and, when it exceeds HIDDEN_LOST_MS, returns `{ kind: "idle", lost: true }` and stops (scanMachine.ts:132) — the `idle → cameraStart` branch below it (scanMachine.ts:134-136) is only reached when the machine was *already* idle. The visibility event that detects the >30 s gap is itself the "next visibility", so §6.3's "re-request on next v | Unit, against the reducer: `run([{type:"hidden",atMs:100},{type:"visible",atMs:100+30001,secureContext:true}], streaming())` yields `{kind:"idle",lost | FIX | fixed | af16a08 |
| A04 | S2 | constants | §7 item 5 ("Constants from §4 are covere | **The §4.6 hint list is defined twice (app and bench) and pinned by no test** (found independently by 2: spec,tests). §4.6's POSSIBLE_FORMATS list + TRY_HARDER exists as source in two places: `buildHints()` in src/features/scan/useScanner.ts:60-70 and `BENCH_FORMATS`/`buildHints()` in bench/decode.ts:49-66. bench/decode.ts:11-15 states the duplication outright ("They are duplicated from src/features/scan/useScanner.ts ... the two lists are asserted to agree in the bench report header") — but nothing asserts it: b | `grep -rn "POSSIBLE_FORMATS" src bench` returns two independent definitions (src/features/scan/useScanner.ts:62, bench/decode.ts:64); `grep -rln "POSS | FIX | fixed | af16a08 |
| A05 | S2 | scan/feedback | §6.3 ("Success feedback never fires on a | **"Got it ✓" success feedback fires on a check-digit mismatch** (found independently by 2: adversary,field). On the confirmed state the CameraView status line always renders "Got it ✓" in the --ok green, including when the read is being held back by the D03 check-digit gate. §6.1 defines the screen change as the primary success feedback and explicitly demotes the beep/haptic to secondary; §6.3 then says success feedback never fires on a mismatch. The beep and vibration ARE correctly withheld (ScanScreen. | CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome npx playwright test --config /tmp/probe/pw.config.ts b-badcheck — drives Chromium's f | FIX | fixed | af16a08 |
| A06 | S2 | storage | §5.2 (scanEvents `deviceLabel: string |  | **Scan events never record the device label; every locally created scanEvents row stores deviceLabel: null**. `upsertVehicle` accepts an optional `deviceLabel` and writes `input.deviceLabel ?? null` (src/lib/storage/upsert.ts:20, 95), but the single write path for both the camera and the keyboard never passes it: useVinCommit.ts:61-67 supplies only vin/origin/symbology/raw/checkDigitValid. `getSettings()` is already awaited on the scan path (ScanScreen.tsx:60) and again inside `kickDecode` (useVinCommit.t | tests/e2e/camera.spec.ts already reads scanEvents out of IndexedDB; set a device label in /#/settings first, then scan: the stored row has `deviceLabe | FIX | fixed | af16a08 |
| A07 | S2 | scanner | §6.3 (streaming / stream_lost), §4.10 Sc | **A fatal ZXing decode error silently ends the scan loop: the camera stays live, the machine stays in `streaming`, and nothing ever decodes again**. `handleResult` takes only the result argument and returns immediately when it is undefined (useScanner.ts:160-163), so every error ZXing reports is discarded. Swallowing NotFoundException is correct and intended (it fires on nearly every frame — S1 report). But @zxing/browser's loop treats the three recoverable exceptions differently from everything else: NotFound/Checksum/Format reschedule the lo | Read path: useScanner.ts:160-163 drops the error argument unconditionally; BrowserCodeReader.js:1131-1135 only reschedules for ChecksumException/Forma | FIX | fixed | af16a08 |
| A08 | S2 | scan/layout | §6.3 (mismatch banner with Rescan primar | **The §6.3 Rescan / Use as-is decision is below the fold on every phone viewport tested**. When the machine confirms a read whose check digit fails, §6.3 requires the user to choose Rescan or Use as-is before anything is written. Both buttons are rendered outside the visible scroll area on every phone-sized viewport measured. The camera is released in the confirmed state, so the preview stays as a ~470 px black rectangle that pushes everything down; the h1, the black preview, the status | CHROMIUM_PATH=... npx playwright test --config /tmp/probe/pw.config.ts d-small. Measured top of Rescan/Use as-is vs main.clientHeight (the scroll view | FIX | fixed | af16a08 |
| A09 | S2 | scan/state-machine | §6.3 (cooldown: "the same VIN confirmed  | **A failed write records the §6.3 cooldown, so the offered "Scan again" is dead for 10 seconds**. handleUseAsIs calls accept(pending.vin) BEFORE awaiting the write. accept dispatches { type: "accepted" }, whose only job (scanMachine.ts:147-153, comment: "The caller reports a persisted read; the cooldown keys on this alone") is to record the 10 s cooldown. If the upsert then fails, nothing is persisted, the user is correctly shown "Couldn't save this VIN" with a "Scan again" button — but the co | CHROMIUM_PATH=... npx playwright test --config /tmp/probe/pw.config.ts c-repro. Test B: scan the bad-check label, patch IDBObjectStore.prototype.put t | FIX | fixed | 8082027 |
| A10 | S3 | ui | §6.1 ("Torch button on the scanner **whe | **The torch button is shown when the track reports `torch: false`, not only when it reports a torch**. The gate is a key-presence test — `capabilities !== undefined && "torch" in capabilities` (useScanner.ts:254) — but the file's own type declares the capability as `torch?: boolean` (useScanner.ts:42-44), i.e. the value can be `false`. A browser that reports `{ torch: false }` for a camera without a lamp (Chromium reports the key on platforms that support the setting, including front cameras that c | Read: useScanner.ts:254 vs the `torch?: boolean` declaration at useScanner.ts:43. Simulate by stubbing `track.getCapabilities()` to return `{ torch: f | FIX | fixed | af16a08 |
| A11 | S3 | perf | §9-S1 ("Performance target: confirmed re | **ZXing's 500 ms scan delays are left at their defaults, capping the scanner at ~2 decode attempts/second and putting the §9-S1 two-second target at risk**. `new BrowserMultiFormatReader(buildHints())` is constructed with no options (useScanner.ts:230), so it takes the library defaults `delayBetweenScanAttempts: 500` and `delayBetweenScanSuccess: 500` (node_modules/@zxing/browser/esm/readers/BrowserCodeReader.js:63-64, applied at :1122 and :1131). Two consequences. (1) The floor for a confirmed read is first-hit + 500 ms + decode time, on top of camer | Read: useScanner.ts:230 passes no `IBrowserCodeReaderOptions`; BrowserCodeReader.js:63-64 supplies 500/500 and BrowserCodeReader.js:1121-1131 schedule | FIX | fixed | af16a08 |
| A12 | S3 | ui | §6.1 ("VIN display: monospace, ≥ 28 px o | **The candidate VIN on the scanner is rendered at 18 px, below the §6.1 minimum for a VIN display**. CameraView renders the in-progress read with `size={state.kind === "confirmed" ? "lg" : "md"}` (CameraView.tsx:156), and `md` is `text-[18px]` (src/ui/VinDisplay.tsx:88) against `lg`'s 28 px. The candidate is precisely the moment the field user is checking the digits against the sticker, at arm's length, in glare — the situation §6.1's floor is written for. Flagged S3 rather than S1 on the honest  | Read: CameraView.tsx:156 with src/ui/VinDisplay.tsx:86-89. On a phone-width viewport, hold a label until "Reading… hold steady." appears — the VIN und | FIX | fixed | af16a08 |
| A13 | S3 | ui/tokens | §6.1 ("Contrast ≥ 7:1 for body text") | **Danger-tone body text measures 6.11:1, below §6.1's 7:1 floor**. --danger #ff6b6b rendered by Banner's title line (Banner.tsx:69, text-lg font-bold = 18px/700) on --bg-elev #161d26 computes to 6.11:1. It is the only text anywhere I measured that falls below 7:1, and it carries the two most alarming messages on the scan path: the §6.4 insecure-context line "Camera needs a secure (https) connection." (CameraView.tsx:45) and the write-failure title "Couldn't save  | CHROMIUM_PATH=... npx playwright test --config /tmp/probe/pw.config.ts d-small (last test) and a-stream ("insecure context"). Computed from getCompute | FIX | fixed | af16a08 |
| A14 | S3 | sheet/actions | §6.1 ("Targets: ≥ 48 px everything; ≥ 56 | **Copy buttons on the Sheet are 48 px; §6.1 requires ≥ 56 px for Copy**. Copy VIN, Copy summary, Copy link and Copy JSON are all variant="secondary", which pins min-h to var(--tap) = 48px (Button.tsx:20). §6.1 names Copy in the ≥ 56 px list. Share at Actions.tsx:167 is variant="primary" and correctly measures 56 px, so the rule is understood elsewhere in the same component. Measured 48.00 px for each of the four. This is Sheet code (slice S3), outside the S1 file set,  | CHROMIUM_PATH=... npx playwright test --config /tmp/probe/pw.config.ts a-stream ("sheet screen") at 390x844. Bounding boxes: Copy VIN 48px h x 173px w | FIX | fixed | af16a08 |
| A15 | S3 | settings | §7 item 3 (no regression in prior slices | **Settings still tells the user that sound, haptics and auto-decode "change nothing yet"**. The Settings screen renders "These three are stored now but change nothing yet: beep and vibrate arrive with camera scanning, and NHTSA details start loading in the step after that." That was true in S0. It is false now: feedback.ts:227-230 reads settings.sound and settings.haptics on every confirmed scan, and useVinCommit.ts:188 gates the vPIC kick on settings.autoDecode. The screen is telling th | CHROMIUM_PATH=... npx playwright test --config /tmp/probe/pw.config.ts g-light. Rendered /#/settings body text contains: "These three are stored now b | FIX | fixed | af16a08 |
| A16 | S3 | ui/theme | §6.1 ("Dark theme default; light theme a | **The light theme §6.1 promises is unreachable, and its nav text would fail 7:1**. tokens.css defines a complete [data-theme="light"] palette, but nothing in the app can ever select it: index.html hardcodes data-theme="dark", there is no prefers-color-scheme rule, and nothing in src/ sets or reads data-theme (grep returns only comments in QrView.tsx). §6.1 says the light theme is available; it is not. Forcing data-theme="light" in the browser also shows the palette would not mee | grep -rn "data-theme" src/ → only QrView.tsx comments; index.html line 2 is <html lang="en" data-theme="dark">. CHROMIUM_PATH=... npx playwright test  | NEEDS-ZACH | fixed | 2dfa744 |
| A17 | S3 | scan/state-machine | §6.3 ("a second identical normalized VIN | **Both §6.3 time windows are compared one-sidedly, so a backwards clock jump silently makes every already-scanned VIN unscannable**. `isCoolingDown` (scanMachine.ts:76) tests `atMs - acceptedAt <= COOLDOWN_MS` with no lower bound, and the confirmation test (scanMachine.ts:109) tests `sighting.atMs - candidate.atMs <= CONFIRM_WINDOW_MS` the same way. Every timestamp comes from Date.now() (useScanner.ts `accept`/`handleResult`), which follows the wall clock. After a backwards system-clock correction, `atMs - acceptedAt` is large  | bun run test -- src/features/scan/scanMachine.adversary.test.ts — two FAILING tests: "[A-03] reads a VIN again when the clock jumped backwards past it | FIX | fixed | af16a08 |
| A18 | S3 | scan/state-machine | §6.3 ("stream_lost (track ended, tab hid | **A decode landing after the tab is hidden rebuilds the candidate that `hidden` just dropped, and it can then confirm against a single read after the return**. `isLive` (scanMachine.ts:65-66) reports `streaming` as live unconditionally, and the `hidden` branch only demotes an existing candidate to `streaming` — it does not stop the machine accepting decodes. `useScanner` releases the camera on `hidden` via the `wantsCamera` effect cleanup, but ZXing's decode loop calls back synchronously from a timer tick (BrowserCodeReader `decodeContinuously`), so a ti | bun run test -- src/features/scan/scanMachine.adversary.test.ts — two FAILING tests: "[A-04] ignores a decode that lands after the tab went hidden" (s | FIX | fixed | af16a08 |
| A19 | S3 | scan/write-path | §6.3 (confirmed-with-mismatch branch: "U | **The re-entrancy guard on Use as-is reads a captured `saving`, so two activations write two §5.2 scan events for one read**. `useVinCommit.useAsIs` guards with `if (pending === null \|\| meta === null \|\| saving) return;` where `saving` is the state value captured when that callback was memoised, not a value read at call time. Two activations that both start before the first `write()` commits therefore both reach `upsertVehicle`, and §5.2 is append-only: one physical read becomes two scan events and scanCount 2. The sc | CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome bun run test:e2e -- tests/e2e/scan-double-tap.spec.ts — "[A-05] two activations of Us | FIX | fixed | af16a08 |
| A20 | S3 | history/rendering | §4.10 DecodeStatus, P7 ("Fail loudly to  | **One vehicle row with a decode status outside §4.10 white-screens the whole History route**. `const decode = DECODE_CHIP[record.decode.status]` (HistoryScreen.tsx:93) is typed `Record<DecodeStatus, {tone,label} \| null>`, so TypeScript treats the lookup as total; at runtime a status outside the enum returns `undefined`. The guard below it is `decode !== null` (line 108), which `undefined` passes, so `<Chip tone={decode.tone}>` throws during render and React unmounts the whole route — ever | CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome bun run test:e2e -- tests/e2e/corrupt-rows.spec.ts — FAILS. The page body is empty an | FIX | fixed | af16a08 |
| A21 | S3 | scanner | §6.3 (cooldown; "once Use as-is is chose | **Use as-is records the cooldown and fires success feedback before the write lands**. `handleUseAsIs` calls `scanFeedback(settings)` and then `accept(pending.vin)` before awaiting `saveAsIs()`. `accept` writes the §6.3 cooldown entry unconditionally, so when the Dexie write fails (`useVinCommit.write` catches, sets `error`, and returns false) the machine holds a 10 s cooldown for a VIN that was never persisted — while the screen shows "Couldn't save this VIN — Nothing was written". | Code path, not yet executable here: no DOM test environment exists for ScanScreen (see notes). Read src/features/scan/ScanScreen.tsx:78-90 against src | FIX | fixed | 8082027 |
| A22 | S3 | feedback | §6.1 ("short beep … + navigator.vibrate( | **feedback.ts had no tests and sits outside every coverage measurement**. `feedback.ts` is named in the S1 scope and had zero tests. It is also excluded from the coverage gate: vitest.config.ts:11 includes `src/lib/**` plus `scanMachine.ts` only, so the file was never measured and the 100%/98.7% headline said nothing about it. What went untested matters: `scanFeedback` runs inside `ScanScreen`'s commit between the write landing and `accept()` recording the §6.3 cooldown | `npx vitest run --coverage --coverage.include='src/features/scan/feedback.ts' src/features/scan/feedback.test.ts` — was 0 tests / unmeasured, now 100% | FIX | partly fixed | 5fb9125 |
| A23 | S3 | gate | §13.5 ("mutation score ≥ 80% on src/lib/ | **`bun run mutate` does not exist, so the §13.5 mutation clause cannot be run at all**. There is no `mutate` script in package.json and no Stryker package in node_modules or devDependencies, yet CLAUDE.md:26 lists `bun run mutate` among the project commands and §13.5 makes it part of the gate. D08 pinned vitest to 4.1.11 specifically because "Stryker's vitest runner predates it and mutation is required from S2" — the pin was taken, the tool never installed. Mutation is optional for S | `bun run mutate` → "Script not found". `ls node_modules \| grep -i stryker` → empty. `grep -n mutate package.json` → no match; `grep -n mutate CLAUDE. | FIX | open | — |
| A24 | S3 | microcopy | §6.4 (verbatim strings), P7 (every error | **No test pins any §6.4 microcopy string**. `grep` across every unit test and every Playwright spec finds no assertion on a single §6.4 string. "Point at the barcode on the door-jamb sticker.", "Reading… hold steady.", "Got it ✓", "Camera is blocked. …", "Camera needs a secure (https) connection." and the three strings supplied under §0 rule 4 (`STARTING`, `CAMERA_STOPPED`, the `no_camera` line) can all be reworded, truncated or swapped bet | `grep -rn "hold steady\\|door-jamb\\|Got it\\|Camera is blocked\\|secure (https)" tests/ src/ --include="*.test.ts" --include="*.spec.ts"` → the only  | FIX | fixed | c344afd |
| A25 | S4 | microcopy | §6.4 ("Offline at scan: *Offline — VIN s | **The §6.4 offline-at-scan banner is unreachable dead code and duplicates the Sheet's copy of the same line** (found independently by 3: adversary,field,spec). Both writers of `savedOffline` run after the record has been written, and `write()` calls `navigate('/v/…')` before it returns (useVinCommit.ts:78): ScanScreen.tsx:72 runs after `await request(...)` resolved true, and ScanScreen.tsx:87 after `await saveAsIs()`. ScanScreen is a sibling route of the Sheet (src/app/router.tsx:16-17), so it unmounts on that navigation and the banner at ScanScreen.tsx: | Read ScanScreen.tsx:59-88 against useVinCommit.ts:56-82: every path that sets `savedOffline` is downstream of `navigate`. Airplane-mode scan lands on  | FIX | fixed | af16a08 |
| A26 | S4 | scan/microcopy | §6.4 line 467: "Check digit doesn't matc | **The §6.4 check-digit line loses its sentence-ending full stop**. The banner splits the §6.4 string across Banner's title and body, which is a reasonable rendering, but the full stop after "match" is dropped in the process. What actually renders is the title "Check digit doesn't match" followed by the body "Usually a misread — try again." Both the camera path and the manual path do the same. Everything else in the string is byte-exact, including the U+2014 em da | grep -n "Check digit doesn't match" src/features/scan/ScanScreen.tsx src/features/scan/ManualEntry.tsx → ScanScreen.tsx:131 and ManualEntry.tsx:135 bo | FIX | fixed | af16a08 |
| A27 | S4 | scan/microcopy | §6.4 line 468: "This number doesn't use  | **Manual entry renders two §6.4 strings with a curly apostrophe where §6.4 uses ASCII**. ManualEntry.tsx uses &rsquo; (U+2019) in two chips: "Check digit doesn&rsquo;t match" (:101) and "This number doesn&rsquo;t use a check digit." (:108). §6.4 spells both with the ASCII apostrophe, and the Banner in the same file (:135) uses ASCII, so the app is inconsistent with itself as well as with the spec. Rendered DOM confirms U+2019: "Check digit doesn’t match" next to "Check digit doesn't m | CHROMIUM_PATH=... npx playwright test --config /tmp/probe/pw.config.ts a-stream ("manual entry screen"). Rendered body text at the bad-check-digit sta | FIX | fixed | af16a08 |
| A28 | S4 | settings | §7 item 3 (no regression in prior slices | **Settings still reports Build: Slice S0**. The About block hardcodes the build label "Slice S0". Three slices have shipped since. Cosmetic, but it is the one place a user or Zach would look to tell which build is on a phone during the §7 item 4 device matrix, which makes it actively misleading during the manual pass hardening depends on. | grep -n "Slice S0" src/features/settings/SettingsScreen.tsx → line 279. Rendered /#/settings body text ends "App VIN Relay / Version 0.1.0 / Build Sli | FIX | fixed | af16a08 |
| A29 | S4 | tests | §13.5 ("A suite that kills mutants is th | **The existing property generators explore far less of the machine than their maxLength suggests**. scanMachine.test.ts's two properties use `fc.array(actionArb, { maxLength: 40 })` with fast-check's default size bias, which generates mostly very short arrays, and stamp every action with an independent random timestamp rather than a clock that moves forward. The combination means generated sequences rarely reach a candidate and essentially never reach the state where a cooldown decides anything. | Verified with a throwaway harness (a switchable-fault copy of the reducer, since I may not mutate src/) run this round: seven faults, six killed by th | FIX | fixed | 1b7c819 |

## NEEDS-ZACH

Delivered as a list, per §13.6 criterion 5. The loop never resolves these itself.

### Z1 (S1) — RESOLVED by Zach: §4.2 now requires the winning VIN to be unique

**This is a false accept, and §13.6 criterion 4 requires zero.** Verified directly, not just reported:

```
extractVin("B1HGCM82633A004352")        -> B1HGCM82633A00435   checkDigitValid: true
extractVin("UNIT B\n1HGCM82633A004352") -> TB1HGCM82633A0043   checkDigitValid: true
extractVin("ſ1HGCM82633A004352")        -> S1HGCM82633A00435   checkDigitValid: true
```

§4.2 step 1 strips whitespace *before* step 2 splits into runs, so a multi-field label or 2D payload becomes one run. Step 4(a) then prefers a window aligned to the run's start, and that straddling window passes the check digit about one time in 33. For the §4.11 fixture VIN, 4 of the 33 alphabet characters (B, K, S, 2) break it as a single-character prefix; measured over random `<prefix> <VIN>` payloads the rate is 1–6%.

Nothing downstream can catch it: the two-read rule agrees because a 2D code decodes identically every frame, and §4.3's gate is satisfied because the check digit genuinely validates. The record is written, beeped, and shown as fact — an N2 violation reached through a §4 constant.

`toUpperCase`, which step 1 mandates, also maps non-ASCII *into* the §4.1 alphabet (`ſ`→`S`, `ß`→`SS`), so a UTF-8 payload can grow the run and trigger the same straddle.

This is the same mechanism D14 closed for the app's own carriers, left open for every other payload. **No agent may fix it** — §4.2 is a §4 constant (CLAUDE.md rule 2, §13.6 hard stop). Options, all of which change §4.2 and need a bench re-run:
- (a) prefer a whole-run window, then a run-**end**-aligned one, before a run-start one;
- (b) require the chosen window to be the only check-digit-valid window in its run;
- (c) stop step 1 joining runs across whitespace, so multi-field payloads split.

Option (b) is the most conservative: it turns an ambiguous run into `NO_VIN` rather than a guess, which is what N2 argues for.

**Zach chose (b), and it is applied.** §4.2 step 4(a) now reads: if exactly one *distinct* VIN among the windows has a valid check digit, that VIN; more than one and the run is ambiguous, so `NO_VIN`. Uniqueness is by VIN, not by window — the same VIN at two offsets is one answer. Step 4(b) still counts *windows*, because an identifier with no check digit is only locatable when it is a run of its own, and a long run of repeated characters collapses to one distinct string without becoming any less of a guess.

Measured after the change: **0 of 33** legal leading characters produce a wrong VIN, down from 4; and 0 of 2,000 random `<field> <VIN>` payloads, down from 1–6%. The four characterisation tests that pinned the hazard now assert it is closed and stand as the regression guard.

What it costs, stated plainly: a run holding more than one plausible VIN is now refused rather than resolved. `1HGCM82633A0043531HGCM82633A004352` was returning the real VIN and now returns `NO_VIN`, and so does the same VIN printed twice — which contains three *spurious* straddling windows that also validate, so the old rule was returning the right answer there by luck rather than by reasoning. The user rescans or types. §4.11 records both.

### Z2 (S2) — RESOLVED: §13.4's tiers are now ordered

See B1. `moderate` blurs and `severe` does not, so moderate measures harder than severe on every 1D symbology. §13.6's 99/90/70 ladder assumes severe ⊃ moderate. Adding blur to `severe` would fix it, but that is a §13.4 change.

### Z3 (S3) — APPLIED AS APPROVED, AND INERT. Superseded by Z5

See B3. The glare band is sized to the image diagonal, so it covers far more of a square symbol. Either exempt 2D from the severe threshold, size the band to the symbol, or accept 0%.

### Z4 (S3) — the light theme §6.1 promises is unreachable

§6.1 says a light theme is available, but §6.2's Settings list has no row for it and nothing reads `prefers-color-scheme`, so the light palette in `tokens.css` is dead code. If it is switched on, `--accent` and `--fg-muted` in the light block need re-checking against the 7:1 floor.

**RESOLVED — approved and built (`2dfa744`).** `theme` joins the §5.6 row as dark / light / system, defaulting to dark, with an unknown stored value falling back rather than throwing. `src/ui/theme.ts` is the only writer of `data-theme`; `resolveTheme` is pure and tested, `applyTheme` subscribes to `prefers-color-scheme` only for `system` and returns a teardown. `index.html` keeps `data-theme="dark"` so the first paint is dark whatever is stored.

The palette is where the work was: **eight of twelve light body-text pairs failed 7:1; none does now.** On `--bg` / `--bg-elev`: `--fg-muted` 7.40 / 6.70 → 7.98 / 7.23 · `--accent` 6.24 / 5.65 → 7.96 / 7.21 · `--ok` 6.80 / 6.16 → 7.90 / 7.16 · `--warn` 7.34 / 6.65 → 7.91 / 7.16 · `--danger` 6.54 / 5.92 → 7.94 / 7.19. Hue and saturation unchanged; only lightness moved, which is what §12 permits. The prediction in this entry that the nav would fail is confirmed — active nav text 5.65 → 7.21. Dark was measured too and needed nothing (worst 7.48). Every ratio is now recorded beside its token in `tokens.css`.

Two consequences went elsewhere: the spec text (§5.6, §6.2 — `35034ca`, including the ruling that `theme` does **not** sync, since a bay phone and a desk laptop want different answers), and **R4-C**, the guide box, which was the one place `--accent` escaped onto live video.

### Z5 (S3) — the §13.4 severe tier stacks six degradations and nothing survives all of them

Detail and the measured options are under "Round 2 (bench)" below. Summary: Z2 ordered the tiers, which is what it was for, and the cost is that `severe` now misses 70% in every cell — `qr_code` at 0/200. Changing which degradations `severe` applies, or exempting 2D from the 70% floor, is a §13.4 change.

**Also on this list because I did not ask first:** I reduced `cylinderTheta` from a 26–40° arc to 9–17° on my own initiative while isolating the 2D failure. It is a §13.4 constant. It is in the tree today, it helped 1D (`code_39` severe 0% → 20%) and it did not fix QR. Keep it or revert it — either way the decision is yours, not mine.

**RESOLVED — approved; option 1, a seeded per-sample subset draw at two of four (`c25bc64`, spec updated in `35034ca`).** 50% scale and heavier grain stay on every frame; warp, glare, low light and JPEG are drawn two at a time off the existing seed stream, so the bench stays bit-reproducible. Ordering holds **by construction** — every severe frame carries every moderate axis at or above its moderate severity plus at least one axis moderate never applies. That required adding moderate's ±15° roll to severe, which had substituted the warp for it; without it a frame drawing glare+jpeg would have had no pose degradation at all and been *easier* than moderate.

Two rather than four because two is the largest draw that still measures. Sweep (40 VINs, mean over subsets of each size, code_39 / code_39_i / code_128 / data_matrix / qr_code): 4-of-4 `5.0 0.0 42.5 42.5 0.0` · 3-of-4 `23.1 27.5 47.5 54.4 21.9` · **2-of-4 `47.5 55.0 63.8 59.6 45.0`** · 1-of-4 `60.6 81.9 71.3 66.3 71.3` · none `67.5 85.0 75.0 100.0 100.0`. A draw of 1 puts code_128 and qr_code over §13.6's 70% — by dilution, not robustness, since QR is 0% whenever warp is drawn. That is threshold-fitting and it was rejected.

Severe before → after: code_39 20.0 → 51.0 · code_39_i 11.5 → 45.5 · code_128 56.5 → 59.0 · data_matrix 55.5 → 55.0 · qr_code 0.0 → 43.5. Clean and moderate byte-identical. **Severe still misses 70% in all five cells** and the report says so.

**The cylinder explanation in this entry was wrong, and it stood for two rounds.** With the keystone pinned to zero and the arc swept to 0.45 rad, qr_code holds 97.5–100% — the cylinder is nearly free even at 26°. What kills QR is the **keystone**: 100% at 0.06, 20.8% at 0.08, **0% from 0.10** (~15° off-axis), while 1D holds through 0.28 and Data Matrix through 0.20. Root cause is in `@zxing/library`: the QR `Detector` estimates the fourth corner as `topRight − topLeft + bottomLeft` — an affine guess at a projective quantity — and falls back to a three-finder-pattern transform that cannot represent a projective map. Now in §13.7, where it belongs. `cylinderTheta` stays reduced (Zach's call), but the *reason* recorded for it was mine and was mistaken.

### Z6 (S1) — §4.2 fabricates a check-digit-valid VIN out of an identifier that carries no check digit

**New in round 2, and it is a false accept: §13.6 criterion 4 requires zero.** Found by the round-2 reviewer, verified directly here:

```
extractVin("PIN JCB4CX00CJ2345678") -> NJCB4CX00CJ234567   checkDigitValid: true

run: PNJCB4CX00CJ2345678 (19 chars)
  window 0: PNJCB4CX00CJ23456   check digit invalid
  window 1: NJCB4CX00CJ234567   check digit VALID   <- returned, and it is the wrong answer
  window 2: JCB4CX00CJ2345678   the real PIN

checkDigitApplies("JCB4CX00CJ2345678") = false   (position 9 is "C")
```

**Z1 cannot reach this case, by construction.** Z1's uniqueness rule works because a real VIN validates and therefore *competes* with the straddling windows: two distinct valid VINs make the run ambiguous, and §4.2 step 4(a) refuses. An off-highway PIN carries no ISO 3779 check digit at all (§4.3, `checkDigitApplies` false), so it can never enter that contest. Exactly one window validates — a window that is not the identifier — and step 4(a) returns it as fact.

Step 4(b) never runs, because 4(a) succeeded. The identifier is not "refused for want of a check digit"; it is **replaced** by a fabricated one and marked valid.

**Rate, measured on synthetic labels** — position 9 forced to a letter other than `X` so the identifier provably carries no check digit, prefixes drawn from `PIN `, `UNIT B `, `SN `, `P/N `, `ID: `, `A `, `MDL 4CX `:

| payload shape | trials | real PIN | refused | **fabricated, marked valid** |
|---|---:|---:|---:|---:|
| `<field> <PIN>` | 2,000 | 15.3% | 79.5% | **5.1%** (103) |
| `<field> <PIN> <field>` | 5,000 | 3.9% | 85.0% | **11.1%** (554) |

A trailing field roughly doubles it: every extra character adds another window and another one-in-eleven chance to validate. The first row is reproducible from the suite — `[R2-F]` in `src/lib/vin/extractVin.adversary.test.ts` — rather than from a scratch script.

Samples: `PIN FWL924R0PEWRU69ZM` → `NFWL924R0PEWRU69Z`; `SN 492CKG3GTBLGCB8FS 01` → `SN492CKG3GTBLGCB8`; `P/N UTZL1EH0VH25XYL4Z USA` → `NUTZL1EH0VH25XYL4`.

**Why this matters more than the number suggests.** §4.7 puts off-highway machines in scope, §4.3 exists specifically so those vehicles are not told on every scan that the read is wrong, and the instruction driving S1 was to read *every* vehicle, not just heavy trucks. This defect is aimed precisely at the population §4.3 was written to protect. Nothing downstream catches it: the two-read rule agrees (a 2D code decodes identically every frame), §4.3's gate is satisfied (the check digit genuinely validates on the fabricated window), and the record is written, beeped and shown as fact — N2 violated through a §4 constant, the same mechanism as Z1 through a door Z1 left open.

**The spec's own prose already says the intended behaviour, and the constant does not implement it.** §4.2's "Known limit" paragraph reads: *"A run that contains more than one plausible VIN yields `NO_VIN`, and so does an identifier carrying no check digit unless it is a run of its own."* That is exactly right, and it is not what the code does — because it is not what step 4(a) says. The prose describes 4(b); 4(a) fires first and never consults it.

**No agent may fix this** — §4.2 is a §4 constant (CLAUDE.md rule 2, §13.6 hard stop). Options, all of which change §4.2 step 4(a) and need a bench re-run:

- **(a) Require the winning window to be a run of its own when it is the only validating window in a run that holds more than one window.** Narrowest possible change: a single-window run (the normal door-jamb barcode) is untouched, and a multi-window run only resolves when the evidence is stronger than one chance-passing window. Cost: a genuine VIN printed with a stray legal character beside it, whose straddles all fail, goes from resolved to `NO_VIN`. Rate to be measured before adopting.
- **(b) Refuse any run whose window count exceeds one and which yields exactly one validating window that is not run-aligned at either end.** Keeps end-aligned reads (`I`-prefixed labels already split on the `I`, so this mostly covers trailing separators the decoder swallowed). More surface, more special cases.
- **(c) Accept it and make it visible instead: return the identifier with `checkDigitValid: false` when the validating window is not the whole run.** Turns a silent false accept into the §6.3 mismatch banner. Contradicts §4.3's promise that a no-check-digit vehicle is not told its read is wrong.
- **(d) Do nothing, and record the limit.** 11.1% of prefixed off-highway reads show a wrong number as fact. I do not recommend this and am naming it only so the list is complete.

My recommendation is **(a)**, for the same reason (b) won Z1: it converts a guess into a refusal, which is what N2 argues for, and it is the smallest rule that closes the class rather than a sample of it.

**RESOLVED — approved, but not by any of the four options above (`04df844`).** Option (a), the recommendation, **breaks a §4.11 fixture**: §4.11 requires `1HGCM82633A0043521` (18 chars, no `I`) to "extract the valid 17-window", and under (a) that two-window run yields `NO_VIN`. §4.11 is the constant that defines correct behaviour, so the rule had to be found by measurement rather than chosen from the list.

**The rule as shipped:** a check-digit-valid window may be returned only if **every** grammar-valid window in its run satisfies §4.3 `checkDigitApplies`. Z1's uniqueness, step 4(b) and `NO_VIN` are untouched.

The argument is sharper than the one in this entry. Z1 works because a competing window is *refuted* when it fails its check digit. A window whose position 9 is a letter was never **tested**, so it is not refuted — it is untested, and a run holding one is genuinely ambiguous between "a VIN with a stray neighbour" and "a printed no-check-digit identifier plus a chance-validating overlap". N2 refuses that. Scope is per **run**, measured rather than assumed: whole-payload scores identically on safety (both 0.0%) but drops a §4.2 covered case — a 2D code carrying JSON with a long second field falls from 90.7% to 7.6%; per-run keeps it at 90.7%.

**The repro above is wrong in a way that mattered.** `I` is not a §4.1 character, so `PIN JCB4CX00CJ2345678` splits into `P` and an 18-char run, and the fabricated window sits at **offset 0 — start-aligned**. That is why options (b) and the start-aligned variant cannot close the headline case: measured, they leave 2.4% and 1.1%, both including this exact payload.

| rule | §4.11 fixtures | fabricated | legit reads |
|---|---|---:|---:|
| today | 19/19 | 6.7% | 93.6% |
| (a) window = whole run | **18/19** | 0.0% | 44.9% |
| (b) aligned to either end | 19/19 | 2.4% | 71.2% |
| start-aligned | 19/19 | 1.1% | 59.7% |
| **per-run testability — shipped** | 19/19 | **0.0%** | 48.3% |

**The reach cut is real and lands where §4.2 says it should.** Unchanged at 100%: a bare 17-char code, a leading ANSI `I` label, a VIN inside JSON — the three shapes §4.2 names as covered and the two that describe a real door jamb. What collapses is undelimited multi-field text: `HONDA ACCORD <vin> 2003` 83.7% → 0.1%, because step 1 strips whitespace before step 2 splits into runs. §4.2's own Known limit already says such a payload "is refused rather than guessed at; the user rescans or types". Wrong reads stay at 0 under every rule and shape.

Z6's population is **closed**: no-check-digit identifiers 8.65% → 0.00%. The residue is a different population and is now **R4-A**. The five characterisation tests are flipped and were each proved to guard the rule by aliasing a reconstructed pre-Z6 `extractVin` under vitest — exactly those five fail against it, the other ten pass.

### Z7 (S4) — §6.4 names the mismatch buttons Rescan / Use as-is; the typed path renders Edit / Use as-is

§6.4: *"Buttons: **Rescan** / **Use as-is**"*. `ManualEntry.tsx:138` renders **Edit**. §6.3 says manual entry takes "the same downstream path", and it does — but there is no scan to repeat on a typed VIN, so "Rescan" would be wrong there and "Edit" is what the screen can actually offer.

This is §6.4 being under-specified for the manual variant rather than the code deviating, which is why no agent should touch it either way. Either §6.4 gains a manual-path line, or the code changes. Smallest reasonable answer: add the line, keep **Edit**.

**RESOLVED — Zach approved the list; the line is in §6.4.** No code changed: `ManualEntry.tsx` was already right and the spec was the thing that was short. The §6.4 entry now reads that on the typed path the banner is the same and the primary button is **Edit**, because there is no scan to repeat and the field the user typed into is still on screen.

### Z8 (S3) — §6.6's wide layout does not exist

Measured at 1280×900: `"/"` resolves to `#/scan`, though §6.6 makes History the default at ≥ 900 px; `document.querySelectorAll("table").length` is **0**; History's container is 672 px wide on a 1280 px viewport (`max-w-2xl`), with no right-hand Sheet pane, and the Sheet is still a separate route.

Keyboard reachability and focus rings are fine — this is the layout itself. It is a missing prior-slice feature, not an S1 regression, so §13.1 puts it on this list rather than in front of the fixer. Build it, or record it as deferred; either is fine, but it should not keep being discovered.

**RESOLVED — deferred, and it was never late.** §6.6 is **S4 scope by the spec's own slice list**: §9/S4 carries the line *"**Wide layout** (§6.6): table + side pane, keyboard-reachable; History is the default route at ≥ 900 px"*. Nothing before S4 was ever supposed to build it, so the auditors that keep finding it are measuring an S4 deliverable against an S1 tree. Building it now would be starting S4 without a `start S4` trigger, which CLAUDE.md rule 1 forbids and rule 3 puts in its own session.

This entry exists so the next round's auditors stop rediscovering it. §6.6 is not a defect until S4. Zach approved the NEEDS-ZACH list wholesale and this is the resolution that list itself offered as the alternative to building it — say `start S4` and it gets built with the rest of that slice.

### Z9 (S3) — §6.3's `candidate` never lapses

§6.3 gives the two-read window 1.5 s, but nothing leaves `candidate` when it expires — only another decode, a hide, a dead track or an error does. Lower the phone after a single read and the screen keeps saying *"Reading… hold steady."* indefinitely over a live preview of nothing, which in the dark reads as "keep holding" for a beep that will never come.

Function is unaffected: each new sighting refreshes the candidate, and a stale one can never confirm. The reason it is here rather than in the fixer's queue is that a lapse needs a **timer tick the pure reducer has no action for** — adding one is a §6.3 change, and §6.3 is a spec section, not a §4 constant, but it is still scope. Cheapest version: a `tick` action the hook fires while a candidate is live, which the reducer turns back into `streaming` after `CONFIRM_WINDOW_MS`. Say whether you want it.

**RESOLVED — approved and built as proposed.** The reducer stays pure (P3): it compares the stamp carried on the `tick` action against the standing candidate with the existing `isWithin` helper and the existing `CONFIRM_WINDOW_MS`, so no clock is read and no constant is restated (§7 item 5). The timer is the hook's, armed at `candidateAtMs + CONFIRM_WINDOW_MS + 1 − Date.now()` and cleared on unmount, on any change of candidate, and when the tab hides. The comparison is two-sided, matching A-17: a tick stamped *before* the candidate means the clock stepped back, and that candidate can no longer confirm in either direction.

One narrow case survives, stated rather than hidden: on an engine that coarsens `Date.now()` heavily (Firefox with `resistFingerprinting` clamps to 100 ms) a tick fired 1 ms past the deadline can read as inside the window and the line persists. That is today's behaviour, so not a regression, and no browser on §7 item 4's matrix coarsens that far.

## Gate history

| round | typecheck | lint | unit | e2e | coverage (lines/branches) | bench | notes |
|---|---|---|---|---|---|---|---|
| baseline | pass | pass | 548 | 16 | 100% / 98.7% | not yet built | state at the end of S3 |
| 1 (audit) | pass | pass | 548 | 16 | 100% / 98.7% | 0 false accepts; 6 threshold cells missed | bench thresholds are not yet a trustworthy verdict — see B1 and B2 |
| 1 (fix) | pass | pass | 613 | 20 | 100% / 98.7% | 0 false accepts; 6 cells missed | 20 findings fixed; reviewer REJECTED the diff on 4 counts |
| 1 (review fixes) | pass | pass | 613 | 21 | 100% / 98.7% | re-running full | N-01 to N-04 addressed; N-02 verified empirically |
| 2 (audit) | pass | pass | 613 | 21 | 100% / 98.7% | 0 false accepts; 9 cells missed after Z2 | tiers ordered, so severe is now the hard tier it was always supposed to be |
| 2 (fix) | pass | pass | 675 | 27 | 100% / 98.7% | not re-run | reviewer REJECTED the diff on 4 counts |
| 2 (review fixes) | pass | pass | 678 | 27 | 100% / 98.7% | 0 false accepts; 9 cells missed, decode rates bit-identical | R2-F escalated as Z6; the R2-G flake diagnosed and fixed; three consecutive clean e2e runs |
| 3 (audit) | pass | pass | 721 of 728 | 27 of 29 | 100% / 99.68% | not re-run | red by exactly the nine repros the adversary wrote; four auditors, one tree, one port — see R3-T8 |


## Round 1 review (§13.3 step 3)

The reviewer **rejected** the fix diff on four counts, having measured the behaviour of the parent and head trees rather than reading the code. All four are addressed.

| id | sev | finding | resolution |
|---|---|---|---|
| N-01 | S2 | `bench/report.md` was overwritten by a `bench:quick` run — 120 attempts, not the 3,000 §13.4 specifies. It attested "0 false accepts" over a corpus 25× smaller than the ledger claims, and erased the evidence B1 rests on (`code_128` read 100/100/100, hiding the moderate-harder-than-severe inversion). | Full 200-VIN run regenerated and committed. |
| N-02 | S3→S2 | **A regression the round-1 fix introduced.** `Use as-is` recorded the cooldown *before* awaiting the write, and because the store now outlives the screen, a failed write locked the VIN out for the full 10 s — measured at 10,757 ms against 1,582 ms before the fix, because the remount used to clear it. The user is shown "Scan again" and the scanner ignores the label. | `useAsIs` now reports success like `request` does; the cooldown is recorded only after the write lands, and a failure recovers through `rescan()`, which records nothing. Verified with a broken `IDBObjectStore.put`: the same VIN saves again in 899 ms. Kept as `tests/e2e/scan-failed-write.spec.ts`. |
| N-03 | S4 | The §6.4 full stop was restored on the scanner path but not the manual one. | Fixed. |
| N-04 | S4 | The build line was replaced with `import.meta.env.MODE`, which renders "production" — less use than the stale string it replaced, on the one screen §7 item 4 needs to identify a build from. | Stamped with the short commit hash at build time. |

Non-blocking observations recorded rather than actioned: `cooldownStore` never sweeps expired entries (bounded by tab lifetime), three Prettier-only hunks in a props test, and a new microcopy string `"Check this read."` that §6.4 does not contain — supplied under §0 rule 4 because a success-green "Got it ✓" beside a mismatch banner contradicts §6.3, and reported here as required.


## Round 2 (bench): the severe tier after Z2 and Z3

Full corpus, 200 VINs, 3,000 attempts. **False accepts: 0.**

| symbology | clean | moderate | severe |
|---|---|---|---|
| code_39 | 100.0% | 77.5% | 20.0% |
| code_39_i | 100.0% | 79.0% | 11.5% |
| code_128 | 100.0% | 81.0% | 56.5% |
| data_matrix | 100.0% | 99.0% | 55.5% |
| qr_code | 98.5% | 97.5% | 0.0% |

**Z2 did what it was for.** The tiers are now ordered — severe ≤ moderate in every cell — so §13.6's ladder is at least coherent. The cost is that severe went from easier-than-moderate to far harder, and now misses 70% everywhere.

**Z3 was inert**, and the premise was mine and wrong: the band is now sized against the symbol's extent along the band normal, but for a square symbol that extent *equals* the diagonal, so no 2D cell moved.

**Z5 (S3, NEEDS-ZACH) — severe stacks six degradations and nothing survives all of them.** I reduced `cylinderTheta` from a 26–40° arc to 9–17° on my own initiative, which was not approved. It helped 1D (code_39 severe 0% → 20%) and did not fix QR. Everything measured, so the next decision is not a guess:

- Each severe component *alone* leaves QR at 100%: 50% scale, blur 0.9, JPEG q40, low light.
- Warp alone: QR is 100% up to a 9° arc, erratic at 17°, 0% at 26°. ZXing fits a planar perspective transform from three finder patterns, and a cylinder is not one.
- At the reduced warp, with glare off *and* blur at 0.5, QR is still 0% — so the residue is warp combined with the lighting, noise and compression stack.
- Rendering QR at 800 or 1100px does not help, so it is not module pitch.

The tier as written is "a blurred, bent, glared, underexposed, downscaled, JPEG-compressed photo" — six bad conditions at once, which is not one bad photo, it is every bad photo. Options for Zach: apply a random *subset* of the degradations per sample rather than all six; set severe thresholds per symbology; or keep severe as an unthresholded stress tier and let §13.6 grade clean and moderate only.

One incidental result worth keeping: `code_39` severe now records 15 misses as `no_vin` rather than `no_decode` — the decoder read something and §4.2 refused it. Under the pre-Z1 rule some of those would have been wrong VINs accepted as fact.


## Round 2

Four auditors, then fixes, then a §13.3 step-3 review that **rejected** the fix diff. Round 2 is **not clean**: it raised new S1 and S2 findings, so §13.6's two-consecutive-clean-rounds counter does not start here.

Ids are prefixed `R2-` and are distinct from the `[R2-nn]` markers inside test files, which were assigned independently by the adversary before triage — a collision recorded here rather than silently renumbered.

| id | sev | area | spec ref | description | repro / test | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| R2-A | S2 | scan/write | §6.3, N1, §5.6, P7 | `ScanScreen` awaited `getSettings()` unguarded on both confirmed paths, so a storage read failure aborted the write before it started: nothing saved, nothing reported, and the success line still on screen. `useVinCommit` already guarded its own read with the comment "a settings read that fails must not fail the save" — the app demonstrating the right pattern one module away from two sites that ignored it. Typing the VIN reported the failure; scanning it did not. | `tests/e2e/scan-storage-failure.spec.ts` | FIX | fixed | c344afd |
| R2-B | S3 | scan/feedback | §6.1, §6.3, P7 | The green success line rendered directly above the "Couldn't save this VIN" banner. Round 1 removed that contradiction for the check-digit gate and left it on the write-failure branch. | `tests/e2e/scan-storage-failure.spec.ts` `[R2-04]`; unit coverage in `CameraView.test.ts` | FIX | fixed | c344afd |
| R2-C | S2 | storage/render | §4.12, P7 | A vehicle row with empty `structural` and `decode` blocks white-screened History and the Sheet. Not corruption: it is what §4.12's `jsonb` defaults produce, so S4's sync will deliver it. | `tests/e2e/corrupt-rows-sync-shape.spec.ts`, `src/lib/storage/normalize.test.ts` | FIX | fixed | c344afd |
| R2-D | S2 | scan/carrier | P6, P7 | An unreadable §4.9 carrier was dropped in silence. The carrier check is what stops `extractVin` fabricating a VIN from the base64url body, so this code is the scanner's to report; dropping it leaves the user pointing a working camera at a code that never resolves. | `tests/e2e/scan-carrier-version.spec.ts` | FIX | fixed | c344afd |
| R2-E | S3 | bench/evidence | §13.4, §13.5, §13.6 crit. 4 | A `--quick` run silently replaced the tracked full-corpus evidence. Third occurrence: round-1 review N-01, again here, and once in the Z1 commit where the message claimed "bench unchanged at zero false accepts" on 25x too little data. | `md5sum bench/report.*` across a quick run | FIX | fixed | bff4baa |
| R2-F | S1 | scan/extraction | §4.2 step 4(a), §4.3, §4.7, N2 | **§4.2 fabricates a check-digit-valid VIN out of an identifier that carries no check digit.** `extractVin("PIN JCB4CX00CJ2345678")` returns `NJCB4CX00CJ234567` marked valid — a straddling window, not the PIN. Z1's uniqueness rule cannot reach it: a no-check-digit identifier never competes, so exactly one window validates and 4(a) returns it. Measured at 5.1% of prefixed and 11.1% of prefixed-and-suffixed synthetic off-highway payloads. Raised by the round-2 reviewer. | `[R2-F]` in `src/lib/vin/extractVin.adversary.test.ts` — flipped to assert the refusal | NEEDS-ZACH | fixed | 21b97c6 |
| R2-G | S2 | tests/e2e | §13.5 ("a gate you do not trust is not a gate") | **The round-1 flake, diagnosed.** `scan-failed-write.spec.ts` injected a failing `IDBObjectStore.prototype.put` from an init script, so the fault landed while Dexie was still opening the connection — and Dexie retries a transaction that fails during open. The retry ran after the test lifted the fault, the write succeeded, the app navigated to the Sheet, and the rest of the test then drove the Sheet's notes form. `not.toHaveURL` hid it: it passes on a navigation that merely has not happened yet. | `npx playwright test tests/e2e/scan-failed-write.spec.ts --repeat-each=4` | FIX | fixed | 1350509 |

### Round 2 review (§13.3 step 3) — REJECTED, then addressed

The reviewer re-ran the whole gate, confirmed no §4 constant moved and that P3 holds, and then rejected on four code grounds. All four are now fixed:

1. **§7 item 5** — the §5.1 pending-decode default was restated in `normalize.ts` instead of imported. `pendingDecode()` is now exported from `upsert.ts` and used.
2. **The structural guard did not close the class it named.** `typeof row.structural.wmi === "string"` tested one field of eleven; a half-populated block passed through and still crashed. Structural is now **rebuilt unconditionally** — it is a pure function of the 17 characters, so rebuilding always agrees with a stored block and needs no guess.
3. **The fix broke its own guarantee.** `buildStructural` threw on a row whose `vin` was absent or malformed, inside the live query, directly under a comment promising one bad row would cost only that row. `normalizeVehicle` now returns `null` for an unrebuildable row and callers drop it.
4. **The bench split was half done** — only the markdown was routed by `--quick`, while the `bench` script hardcodes `--json`, so a quick run still clobbered the tracked JSON through the flag. Both paths split now, verified by md5.

The reviewer also found **a new S1 the fixes did not touch**: §4.2 fabricating a VIN out of an off-highway PIN (R2-F above). It is a §4 constant, so it is not fixed here — it is **Z6** on the NEEDS-ZACH list, with the reproduction verified independently and the rate measured. It went on that list before this loop reported, which is the point of §13.6 criterion 5.

Three of the reviewer's own S3s were also fixed: the version copy no longer asserts "newer" for any `v != 1` (it uses the error's own message, matching the Import route), a storage failure no longer shows the check-digit remedy ("Not saved." rather than "Check this read."), and the carrier banner is cleared on mode switches instead of reappearing about a code no longer in front of the camera.

### Process findings from the review, recorded not dismissed

- **§13.2 role violation.** The orchestrator "never edits `src/` itself", and this round's fixes were written by the orchestrator directly — which is exactly why step 3 was skipped the first time. The `fixer` and `reviewer` agent roles now exist in `.claude/agents/` and should carry the next round.
- **One commit carried four fixes**, against §13.2's one-finding-per-commit; none is independently revertible.
- **Finding ids collide** between the adversary's in-test `[R2-nn]` markers and the ledger. Recorded above rather than renumbered after the fact.
- **The e2e flake is diagnosed and fixed** (R2-G above). It was a test bug, not an app bug: the fault injection raced Dexie's open-time transaction retry, so the "failed" write sometimes landed after the fault was lifted. The test now opens the database first and asserts the failure positively ("Couldn't save this VIN") instead of relying on `not.toHaveURL`, which passes on a navigation that has not happened *yet*. Four repeats and three consecutive full-suite runs are clean, and the spec went from ~30 s of retries to under a second. Recorded because the earlier one-off failures in `smoke.spec.ts` are still unexplained and may or may not be the same shape.

## Round 3

Four auditors in parallel against one working tree. Findings are consolidated under one id space; where two auditors found the same defect independently, that is recorded rather than counted twice.

**Id mapping**, because the auditors numbered independently again and the markers are already inside committed test files. Recorded rather than renumbered after the fact — editing an agent's test files to renumber them is the §13.2 role violation round 2 recorded:

| in-test marker | canonical ledger id |
|---|---|
| `[R3-A]` in `decodeQueue.kick.test.ts`, `scan-decode-kick.spec.ts` | R3-A (same finding, same number by luck) |
| `[R3-B]` in `upsert.concurrency.test.ts`, `insecure-context.spec.ts` | **R3-L** |
| `[R3-C]` in `carrier.adversary.test.ts` | **R3-F** (the spec-auditor found it independently) |
| `[R3-D]` in `carrier.adversary.test.ts` | **R3-M** |
| `[R3-E]` in `upsert.concurrency.test.ts` | **R3-N** |
| `[R3-P1]`–`[R3-P3]` | unchanged — these are passes, not findings |

### Findings

| id | sev | area | spec ref | description | repro / test | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| R3-F2 | S1 | sheet/render | N2, §6.2, P7 | **A sheet-to-sheet fragment navigation leaves the previous vehicle's details and notes on screen, under the current VIN's heading.** `#details-heading` count goes 1 → 2 → 3 → 4 on successive hash changes; a page headed `1FT ZX176 2 T K B12345` renders `MAKE HONDA / MODEL Accord` above `MAKE FORD / MODEL F-150`, with two `#sheet-unit` inputs holding `["TRUCK-A","TRUCK-B"]`. The stale block is dead DOM — React unmounted the fibers, the host nodes stayed — so the old input will not even accept a keystroke. Another vehicle's make, model and unit rendered as fact on this vehicle's sheet is N2, and §13.3 makes an N-rule violation S1. Reachability is narrow and stated plainly: in-app `Link` taps, browser back/forward and history→sheet are all clean; only a sheet→sheet param change driven from outside the router reproduces it. | seed two records, open `/#/v/A` by tap, then click a plain `<a href="#/v/B">`; probe `f-dup2.spec.ts`, `h-dup4.spec.ts` | FIX | fixed | 7a06b5a |
| R3-A | S2 | decode/network | §4.7 ("one request per VIN ever"), §5.4 | **Two entry points into the decode queue, one guard.** `Shell.tsx:11` runs `startDecodeQueue`, whose `running` flag exists precisely to stop §5.4's triggers fanning out into concurrent requests for one VIN — and `decodeQueue.test.ts` already pins that. `useVinCommit.ts:24` calls `runDecodeQueueOnce` directly, outside that closure, on every successful save. Both runs read the same pending rows and each issues a request. On a bad radio both land `applyDecodeResult` with `status: "pending"`, so one round of no signal spends two of §5.4's ten attempts and the row fails in five rounds instead of ten. Found independently by the spec-auditor and the adversary. | `bun run test -- src/lib/storage/decodeQueue.kick.test.ts` (2 FAIL); `tests/e2e/scan-decode-kick.spec.ts` | FIX | fixed | c293866 |
| R3-L | S2 | storage/scan-write | §5.2 (`crypto.randomUUID`), §6.3, §6.4, P7, N1 | **Over plain http nothing can ever be saved, including through the keyboard fallback §6.4 sends the user to.** `crypto.randomUUID` is `[SecureContext]`, so on any non-https, non-loopback origin it is `undefined` and the §5.3 transaction throws. §6.3 routes an insecure context to `error(insecure_context)`, §6.4 gives it a line and `CameraView` offers "Type VIN instead" — the app is *designed* to be opened this way. Reachable from `bun run preview --host` and from any LAN or reverse-proxied deployment without TLS. Filed S2 rather than S1 because it fails loudly and the rollback is clean, but the remedy offered is one the user cannot act on. | `src/lib/storage/upsert.concurrency.test.ts` `[R3-B]` (2 FAIL); `tests/e2e/insecure-context.spec.ts`, which asserts the platform claim before the app's | FIX | fixed | 61853d8 |
| R3-F1 | S2 | scan/layout | §6.1, §6.3, P7 | **The carrier-rejection banner renders entirely below the fold on a phone, so R2-D's fix never reaches the user.** At 360×640: `main.clientHeight` 591, `scrollHeight` 798, `scrollTop` 0; the `[role=alert]` runs 593.5→766 — **0 visible px**; "Keep scanning" 701→749, 110 px below the fold. `elementFromPoint(180, 603)` returns the bottom nav. At 390×844 the button shows **3.3 px of a 48 px target**. All the user sees is the live QR under "Point at the barcode…" — the silent-refusal state R2-D was raised to end. `tests/e2e/scan-carrier-version.spec.ts` passes anyway: Playwright's `isVisible()` ignores the scroll clip, measured `true` at 0 visible pixels. Cause: the machine stays `streaming`, so A08's fix (which hides the preview) applies only in `confirmed`. | 360×640 fake camera, v2 QR, measure `[role=alert]` against `main.clientHeight`; screenshot in the round-3 scratch dir | FIX | fixed | 2888049 |
| R3-F3 | S2 | history/storage | P7, §6.2, §4.12 | **A row `normalizeVehicle` drops disappears from History, from the count and from "Export all", with no message anywhere.** Seeded 3 rows, one with a non-§4.1 VIN written raw the way §4.12's sync will deliver: `getAll()` → 3, header renders "2 vehicles", export note renders "Downloads all 2 saved vehicles on this device". With every row unrenderable, History renders "Nothing scanned yet" while the rows sit in IndexedDB. R2-C's "one bad row costs that row" is right; P7's other half — fail loudly to the user — is missing, and the export claim turns it into a path to real loss: Export all, then Clear all data, destroys a row the UI promised was in "all". | probe `d-history-sheet.spec.ts`; `HistoryScreen.tsx:264-266, 279, 210-211` | FIX | open | — |
| R3-B | S2 | scanner | §9-S1, §0 rule 5 | **Tap-to-refocus was never built and never declared as a deviation.** §9-S1 asks for it behind a `focusMode` capability probe; `grep -rn focusMode` over the whole repo returns one hit, the spec line asking for it. `S1_SESSION_REPORT.md` lists three deviations and not this one. iOS Safari exposes nothing here, but Android Chrome does, and Android Chrome is on §7 item 4's matrix. | `grep -rn "focusMode" --include=*.ts --include=*.tsx --include=*.md .` | FIX | fixed | 1e5205d |
| R3-C | S2 | import/storage | §5.3 ("keep existing `unit`/`notes` unless … the user confirms overwrite") | **An import replaces an existing record's `unit` and `notes` with no confirmation and no sign in the preview that anything will be lost.** `ImportScreen.tsx:436-438` passes them straight through and `upsert.ts:58-59` lets incoming win. The preview cannot be the confirmation §5.3 means: nothing on that screen reads `db.vehicles`, so the local value about to disappear is never shown. `upsert.ts:63` then stamps `metaUpdatedAt = now`, so the import wins §4.12's LWW on every other device in S4. Reachable from S1 by scanning a §4.9 QR — the two-crew handoff §9-S3 describes. For `notes` this is destruction of user-typed text, which §13.3 would call S1; filed S2 because it needs a colliding import rather than a plain scan. | read `ImportScreen.tsx:436-438` → `upsert.ts:58-59`; the preview never reads `db.vehicles` | FIX | open | — |
| R3-D | S3 | scan/state-machine | §6.3 (`requesting → streaming` on `getUserMedia`) | **`streaming` is entered after `decodeFromStream` resolves, but ZXing decodes inside it, so the first decoded frame of every camera start is dropped.** `useScanner.ts:248` resolves getUserMedia; `dispatch({type:"stream_started"})` is at `:289`, after the reader is built. `scanMachine.ts:134` drops a `decoded` that arrives in `requesting`. Costs one attempt out of the four to six budgeted for §6.3's 1.5 s window, on every start, Rescan and return from a >30 s hidden tab. | `scanReducer({state:{kind:"requesting"}}, {type:"decoded",…})` → machine unchanged | FIX | open | — |
| R3-E | S3 | scan/write-path | §6.3, P7, §6.4 | **A write failure's banner survives into the next check-digit hold, so two contradicting banners render together.** `useVinCommit.request` sets `pending` without clearing `error`; `ScanScreen` deliberately calls `rescan()` without `dismiss()` after a failed Use as-is (it must not record the cooldown), so the camera restarts with `error` still set. The same label then re-reads and both banners render — the second about a read that is gone. | break `put` after the Dexie connection is open, scan the bad-check label, tap Use as-is, leave the label up | FIX | open | — |
| R3-F | S3 | constants/carrier | §7 item 5, §4.9, D14, P7 | **§4.9's carrier grammar exists twice with two different URL regexes, and the disagreement drops a recognised carrier in silence.** `carrier.ts:25` and `codec.ts:155` are not the same expression, though the second's comment claims it is "kept in step". `carrier.ts` matches `d=` case-insensitively; `codec.ts` reads it through `URLSearchParams`, whose names are case-sensitive. `#/i?D=…` → `isPayloadCarrier` true, `parseCarrier` null, `ScanScreen.tsx:49` returns without a word — the R2-D symptom, still open on the null branch. Same class the other way: the guard matches the *last* `#/i?`, `parseCarrier` the *first*. Found independently by the spec-auditor and the adversary. | `src/lib/payload/carrier.adversary.test.ts` `[R3-C]` (1 FAIL) | FIX | fixed | bf95090 |
| R3-M | S3 | scan/carrier | §4.9, D14, N2, §13.6 crit. 4 | **The D14 guard is pinned to `VINRELAY1:`, so a text carrier of any other version is handed to `extractVin` and mined.** The URL recogniser is version-agnostic and names the version in its error; the text recogniser is not, so a v2 text carrier is treated as ordinary label text. D14's own comment states the requirement it misses. Measured over 3,000 seeded payloads: **8.3% of carrier bodies reaching `extractVin` yield a VIN, and every one is wrong** — the same rate for a `%23`-escaped URL carrier and for a carrier with a label in front, so it is the base64url body doing it, not the wrapper. A QR decodes identically every frame, so §6.3's two-read rule agrees. Latent today; it arms itself the day `PAYLOAD_VERSION` moves. **Not a §4 change**: v1 stays the only prefix written or decoded, only the guard widens. | `src/lib/payload/carrier.adversary.test.ts` `[R3-D]` (1 FAIL) | FIX | fixed | bf95090 |
| R3-N | S3 | storage/upsert | §5.1, §5.3 | **`upsertVehicle` trusts the type of the `scanCount` it read.** `(existing?.scanCount ?? 0) + 1` on a stored `"3"` yields `"31"`, then `"311"`, and rides out to the §S3 export; a missing `scanCount` silently resets to 1. No live path writes such a row today — import is gated by `z.number().int()` — so this is the write path hardened against exactly the shapes R2-C fixed on the read path, and that §4.12's sync will deliver in S4. `structural` and `decode` are already rebuilt or defaulted on every write; the aggregate is the one field taken on trust. | `src/lib/storage/upsert.concurrency.test.ts` `[R3-E]` (1 FAIL) | FIX | fixed | 61853d8 |
| R3-H | S3 | import/decode-seam | §9-S3 ("preview → confirm → upsert → kick decode") | **The Import route never kicks a decode, and its comment claims it does.** `grep -rn runDecodeQueueOnce src/` finds one caller, `useVinCommit.ts:24`, which Import does not use. The receiving phone in the §9-S3 QR handoff sits on "Fetching details from NHTSA…" for up to §5.4's 60 s poll. S3 rather than S2 because the decode does arrive and N1 is intact. | `ImportScreen.tsx:459-461`; `grep -rn "runDecodeQueueOnce" src/` | FIX | fixed | c293866 |
| R3-F4 | S3 | scan/microcopy | §6.4, P7 | **The write-failure banner prints the raw storage exception to the field user, tripled.** Rendered verbatim: `QuotaExceededError: storage full QuotaExceededError: QuotaExceededError: storage full`, 14 px, on both the camera and manual paths. §6.4's rule is "terse, plain, never blames the user"; an unbounded, untranslated engine string is none of those, and it is the only text on the scan path the app did not write. | probe `a-writefail.spec.ts`; `useVinCommit.ts:83` rendered at `ScanScreen.tsx:209` and `ManualEntry.tsx:126` | FIX | fixed | edb80f5, 0dda470 |
| R3-F5 | S3 | scan/carrier | P7, §6.3 | **"Keep scanning" clears a banner the next decode re-raises, so the state has no durable route forward.** Click it, and six seconds later with the same QR still up the alert is back. The realistic case *is* the other phone still holding the code, so the only exits are "Type VIN instead" or walking away. Related: `carrierError` is never cleared by a subsequent good read, so it also outlives the code it describes. | probe `b-carrier.spec.ts` | FIX | fixed | acaae80 |
| R3-F6 | S3 | ui | §6.1 ("VIN display: monospace, ≥ 28 px on phone … letter-spaced") | **Three further VIN displays measure 18 px, and one is not letter-spaced.** History rows 18px/`tracking .06em`, Import preview 18px, QR view 18px with `letter-spacing: normal` — and the QR view is exactly where a second person reads the number off the screen to key it in. It renders its own `<p>` rather than `VinDisplay`. Same rule as A12, three more instances, in prior-slice files. | probe `m-vin.spec.ts` (`getComputedStyle` on each) | FIX | fixed | 2f3cfef |
| R3-T1 | S4 | vpic/client | §4.7, §13.5 | **The last uncovered branch in `src/lib` is unreachable.** `client.ts:134`'s `outcome.lastError ?? "Unknown error"` cannot fire: every `attemptOnce` return with `result: null` carries a non-null string `lastError`, and the only return with `lastError: null` carries a result. `MAX_ATTEMPTS` is 3, so the `"No attempt was made"` initialiser at `:127` is dead too. A finding about the code, not the tests; the fix is a discriminated union. Verified independently. | `bun run test:coverage` → `client.ts:134`; read all five `attemptOnce` returns | FIX | open | — |
| R3-I | S4 | microcopy | §6.4 ("This number doesn't use a check digit." — the note **on the sheet**) | **The one surface §6.4 names for this string is the one that drops its full stop.** `StructuralBlock.tsx:72` renders it without; `ManualEntry.tsx:107` renders the same sentence with. Found independently by the spec-auditor and the field-auditor (their R3-F8). Same class as A26 and round-1 N-03. | `grep -rn "use a check digit" src/`; probe `l-misc.spec.ts` seeding `WVWZZZ1JZ1W123456` | FIX | open | — |
| R3-J | S4 | constants/microcopy | §7 item 5, §6.4 | **The §6.4 check-digit strings and the write-failure title are literal duplicates across `ScanScreen.tsx` and `ManualEntry.tsx`.** The hazard is recorded history, not theory: round-1 review N-03 was this exact drift, and R3-I is it happening again between `ManualEntry` and `StructuralBlock`. `SheetScreen` and `ImportScreen` already hoist their §6.4 copy into named constants; the scan pair does not. | `grep -rn "Usually a misread\|Couldn't save this VIN" src/` → two files each | FIX | open | — |
| R3-F7 | S4 | sheet/microcopy | §6.4 ("Ambiguous year: *1996 or 2026 — will confirm when details load*") | **The ambiguous-year line renders with no separator at all**: `dd.textContent` is `"1993 or 2023will confirm when details load"`. N2 is satisfied — both candidates shown, neither asserted — this is the wording only. Same class as A26. | probe `d-history-sheet.spec.ts`; `StructuralBlock.tsx:43-57` | FIX | open | — |
| R3-F9 | S4 | ui/a11y | §6.6 ("keyboard-reachable: Tab / Enter / Escape"), N5 | **The QR overlay is `role="dialog" aria-modal="true"` with no focus containment.** All ten Sheet controls behind it stay tabbable; the background is neither `inert` nor `aria-hidden`. Escape closes it and Close is 358×56, so the N5 and §6.1 halves are right. axe does not flag it. | probe `l-misc.spec.ts`; `QrView.tsx:97-102` | FIX | fixed | a865b69 |
| R3-F11 | S4 | scan/layout | §6.1 | **The two halves of the write-failure message are 200 px apart with an unrelated button between them.** At 390×844: "Not saved." at y 62, the VIN at 94.8, "Type VIN instead" at 180.8, "Couldn't save this VIN" at 261.8. Both are on screen at 360×640 too, so this is ordering, not visibility. | probe `a-writefail.spec.ts` → `### RECTS` | FIX | fixed | d83212d |
| R3-T8 | S3 | gate/process | §13.2, §13.5 | **Four auditors ran against one working tree and one preview port, and the round paid for it.** The spec-auditor could not run `test:e2e` at all (a parallel auditor held `:4173` and `playwright.config.ts` sets `reuseExistingServer: false`), and it filed R3-G — a typecheck and lint failure on `src/lib/storage/__probe.test.ts` — which was another auditor's scratch file, created and deleted while it was looking. Two authors also wrote tests concurrently, so no single `bun run test` during the round was a verdict on anything. | `git status --porcelain` during the round; R3-G withdrawn below | FIX | partly fixed | — |
| ~~R3-G~~ | — | gate | §13.5 | **WITHDRAWN — false positive.** Reported as a stray `src/lib/storage/__probe.test.ts` breaking typecheck and lint and inflating the count to 687. It was a concurrent auditor's scratch file, created and deleted mid-audit. The gate was green at 549b522 before that commit and the file does not exist. Kept in the ledger rather than deleted: a withdrawn finding is evidence about the process (R3-T8), and silently removing it would be the third time this round's numbering lost information. | `git log` at 549b522; the file is absent | WONTFIX | withdrawn | — |
| R3-T2 | S4 | tests/scan | §13.5 | A29 quantified and closed — see the round-3 notes below. | `scanMachine.test.ts` `[A-29] generates sessions that reach the states these laws are about` | FIX | fixed | 1b7c819 |
| R3-T3 | S3 | storage/normalize | §4.12, §7 item 5, P7 | `normalizeVehicle`'s three decisions were executed by six tests and asserted by none. Making the structural rebuild conditional again passed the whole round-2 suite — the round-2 review's own rejection able to revert itself in silence. So did reading the year off the clock, and mutating the caller's Dexie row in place. | `src/lib/storage/normalize.test.ts` — three tests, each killing exactly its mutant | FIX | fixed | 1b7c819 |
| R3-T4 | S3 | storage/db | §5.1, §4.12 | `nowIso` — the timestamp on every record on the device — had no test at all, and the suite runs in UTC, so the offset sign was never exercised. A wrong sign yields a well-formed stamp naming an instant twice the offset away, which on §4.12's LWW clock loses an edit to an older one. | `src/lib/storage/db.test.ts` — 7 tests over four zones | FIX | fixed | 1b7c819 |
| R3-T5 | S3 | storage/decodeQueue | §5.4, §4.7, P7, N1 | Two claims with no test: the serial run re-checking `navigator.onLine` each turn so a device losing signal mid-run does not spend the remaining rows' one permanent §4.7 request, and `startDecodeQueue`'s `.catch` keeping the §5.4 poll alive through a storage failure — the only uncovered *function* in `src/lib`. | `src/lib/storage/decodeQueue.test.ts` | FIX | fixed | 1b7c819 |
| R3-T6 | S3 | storage/upsert | §5.6, D11 | `setVehicleMeta` was only ever called with `unit` present, so `undefined → leave it alone` was uncovered. The Sheet edits unit and notes separately, so a patch reading absence as a clear would wipe a truck's unit number every time someone typed a note. | `src/lib/storage/upsert.test.ts` | FIX | fixed | 1b7c819 |
| R3-T7 | S3 | scan/CameraView | §6.1, §6.4, §6.3 | `unsaved` was asserted only alongside a confirmed read, though streaming-with-failure-banner is reachable (a failed Use as-is calls `rescan()` while `error` is still set). A mutant leaking "Not saved." into the §6.4 aiming line survived the whole round-2 suite. | `src/features/scan/CameraView.test.ts` | FIX | fixed | 1b7c819 |
| R3-P1 | — | storage/upsert | §5.3, P4 | **Held.** P4 under real interleaving, not just a repeated tap: twenty writers across four VINs, ten on one VIN, a decode landing mid-upsert in both orders, a settings write and a `clearAllData` crossing a scan write. One row per VIN every time, `scanCount` equal to that VIN's event count, every event id distinct, no orphan in either direction. | `upsert.concurrency.test.ts` `[R3-P1]` — 6 pass | — | fixed | 402ba43 |
| R3-P2 | — | storage/upsert | §5.2, §5.3 | **Held.** §5.3's transaction makes a half-written scan impossible in both directions, and a re-scan that fails leaves the earlier record byte-identical — `lastScannedAt` and `scanCount` do not move for a read that was never stored. | `upsert.concurrency.test.ts` `[R3-P2]` — 3 pass | — | fixed | 402ba43 |
| R3-P3 | — | storage/upsert | §4.12, §5.1 | **Held.** A backwards clock jump mid-session does not corrupt the aggregates; an import carrying an older `at` does not move `lastScannedAt` back or rewrite provenance; a stored timestamp that no longer parses heals rather than propagating. | `upsert.concurrency.test.ts` `[R3-P3]` — 3 pass | — | fixed | 402ba43 |

### What held, measured — do not re-audit

- **axe-core: zero critical, zero serious** across nineteen states, including every state rounds 1 and 2 added. The single moderate (`landmark-unique` ×3) exists only in the R3-F2 state and is a consequence of it.
- **Contrast.** Every text node in every probed state clears §6.1's 7:1. Lowest anywhere is **7.48:1** — A13's `--danger` fix holds, up from 6.11:1.
- **Targets.** Nothing under 48 px in any probed state; A14 is fixed — the four Copy buttons measure 173×56, up from 48.
- **The fold at 360×640.** A08 holds: the check-digit decision is above the fold on both paths, and the camera write-failure state does not scroll at all.
- **N1.** Offline, a typed VIN writes and renders the full structural block in **120 ms**, with zero spinners and the §6.4 offline line verbatim.
- **N5.** Zero `hover:` utilities in `src/`; no pointer, touch or gesture handler anywhere; the only disclosure is a native `<details>`.
- **§4.6 hints are single-sourced** (A04 closed) and the bench measures the app's own list.
- **No §4 constant is re-derived in `src/features/scan`** — the check digit, grammar, extraction, symbology map and carrier test are all imports.
- **P3 holds**: `src/lib/vin/` and `src/lib/payload/` import no DOM, React or I/O.
- **N4**: `isSecureContext` is read on mount, on retry and on every return to visibility, and an insecure context can never reach `getUserMedia`.
- **Camera lifecycle**: both `await` points in `start()` are guarded, `release()` detaches the `ended` listener before stopping, and React 19 StrictMode's double invoke is covered by the same flag.
- **The §6.3 boundaries** are pinned on both sides and at the exact value: six ±1 ms faults, 6/6 killed.
- **The 700-byte cap is measured in bytes**, already pinned exactly.
- **The vPIC client** survived everything thrown at it.
- **Focus ring** is 3 px `#57c7ff` at offset 2. It first measured 1.00:1 against `--bg`; `transition-colors` includes `outline-color`, so a sample at +0 ms reads currentColor. Recorded so nobody re-measures it the fast way.



## Round 4 — Zach approved the NEEDS-ZACH list

`do all of those`. Z4, Z5, Z6, Z7, Z8, Z9 and R3-Q were implemented by five owners with disjoint file sets — a direct answer to round 3, where four agents sharing one working tree produced a false finding (a scratch file left in `src/`), a corrupted coverage run and a port collision. Each entry above carries its own resolution; this section records what the work *found*.

| id | sev | area | spec ref | description | repro / test | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| R4-A | S1 | scan/extraction | §4.2 step 4(a), §4.3, §4.11, N2, §13.6 crit. 4 | **§4.2 still fabricates a check-digit-valid VIN when a *misread* North-American VIN shares a run with a stray legal character.** `extractVin("B1HGCM82633A004353")` → `B1HGCM82633A00435`, `checkDigitValid: true`; 4 of the 33 legal leading characters do it on that fixture. Neither Z1 nor Z6 can reach it, and Z6 cannot **by construction**: every window in such a run has a digit at position 9, so the run *is* settleable and 4(a) settles it on the straddle. It is irreducible while §4.11 stands — `1HGCM82633A0043521` (valid VIN + stray char, which §4.11 **requires** to resolve) and `B1HGCM82633A004353` (invalid VIN + stray char) are byte-for-byte the same shape: a two-window run with exactly one validating window. Closing it means amending §4.11. Nothing downstream catches it: the two-read rule agrees and §4.3's gate is satisfied. | `[R2-01 residue]` in `src/lib/vin/extractVin.straddle.test.ts` — characterisation, passing today, goes red when closed | **NEEDS-ZACH** | fixed | 21b97c6 |
| R4-B | S3 | tests/measurement | §13.2 (fixed seeds), §13.5 | **The house LCG cycles, so every rate this ledger quotes overstates its sample about fourfold.** `seed * 1103515245` reaches ~2.37e18, far above 2^53, so the multiply loses precision in a double and the generator degenerates: **period 10466 from every seed in use** (20260904, 12345, 0x2c6b, 0x5aff), after 13545–16707 distinct states. Verified independently by the orchestrator. At ~21 draws per trial, a test claiming 3,000 trials exercises ~708 distinct payloads and a residue of 13 fabrications is **2 distinct payloads**. Ratios are not necessarily wrong, but Z1's, Z6's in-suite, R2-F's and R3-M's confidence intervals are about twice as wide as stated, and R4-A's residue is far coarser than it reads. | run the LCG 200k steps from each seed and count distinct states | FIX | fixed | 9f5c357 |
| R4-A' | S2 | scan/extraction | §4.2 step 4(a), §13.6 crit. 4 | **R4-A's residue figure was wrong and this is the number to rule on.** Re-measured on a generator that does not cycle: **0.80%, 95% CI [0.73%, 0.87%]** — 487 fabrications over 60,755 misread payloads, from 194,083 distinct payloads — against the recorded **1.34%**, which was 13 hits on **2 distinct payloads** counted six or seven times. Biased about 1.7x high, and the old point estimate lies outside the corrected interval. The finding stands; the number changes. | `src/lib/vin/extractVin.straddle.test.ts` | **NEEDS-ZACH** | obsolete | 9f5c357 c |
| R3-U-b | S2 | ui/tokens | §6.1, §6.6, WCAG 2.4.7 | **The app-wide focus ring outranked every `focus-visible:*` utility in the repo, and had since Tailwind v4 landed.** `src/index.css` ended with an unlayered `:focus-visible { outline: 3px solid var(--accent) }`; Tailwind v4 emits utilities inside `@layer utilities`, and unlayered normal declarations win regardless of specificity. Invisible for two reasons: `Button.tsx`'s values coincide with the global rule's, and CSS layering is invisible to vitest and tsc alike. It broke on exactly one control — the R3-B tap target asked for `outline-offset-[-3px]` because the preview clips its overflow, got `+2px`, and the ring was **clipped away entirely**. Proved in a browser, not from byte offsets: two contradicting utilities on a real button read `solid/3px/2px` before and `none/0px/-3px` after; 22 focusable elements byte-identical across four screens. Every clipping container in `src` audited by inflating each focused box by offset+width against its nearest clipping ancestor's padding box — elements clipped: 0 outside the preview, so `Button.tsx` needed no change. | browser probe + `getMatchedStylesForNode` | FIX | fixed | 139ab11 |
| R4-D | S4 | gate/config | §13.5 coverage include | A test-only helper under `src/lib/` entered the §13.5 coverage denominator, because `coverage.exclude` named only `*.test.ts`, `test-setup.ts` and `types.ts`. Branchless and fully exercised, so it could not lower the percentage — it added covered lines that slightly mask gaps elsewhere, which is the wrong direction for a gate to be wrong in. Excluded by pattern so the next helper does not have to remember. | `vitest.config.ts` | FIX | fixed | 57bfbb3 |
| R4-G | S3 | ui/QrView | §6.6 | The QR overlay does not trap focus: Tab from it walks into the sheet behind it, reaching `Refresh details` and `Copy VIN` before ever reaching `Close`. §6.6 requires Tab / Enter / Escape reachability, and an overlay leaking focus into inert content fails it. Found while auditing R3-U-b. | measured Tab order | FIX | fixed | a865b69 |
| R4-H | **S1** | S3 handoff / QR | §9-S3, §6.1 | **The handoff QR was drawn at device-pixel size on every device with DPR > 1 — every phone — so it overflowed the screen and could not be scanned.** `qrcode`'s renderer ends each draw by sizing the CSS box to the backing store it was handed, correct only at DPR 1, overwriting React's inline style. A 237 px code got a **711 px box at x = −195.5** on a 320 px viewport, clipped both sides, with Close **125 px below the fold**. §9-S3's flow is "show QR, scan QR" and a cropped code does not decode. Desktop is DPR 1, which is why three hardening rounds, a 32-test e2e suite and the §13.4 bench never saw it. Found within an hour of adding Android profiles to the gate. | `tests/e2e/handoff.spec.ts`, red on `galaxy-s9` and `pixel-7` against the old component, green after | FIX | fixed | 8a5b14b |
| R4-H' | S2 | tests/evidence | §13.5 | **The guard watching that feature could not fail.** `handoff.spec.ts` asserted `width > 150` on a size floored at 200 CSS px, so only a vanished canvas could trip it — and it passed at 711 px through three rounds. Replaced with the property: square, whole inside the viewport *and* its dialog, hit-testable at its centre, `backing === round(css × min(dpr, 3))` as an equality so a "fix" that drops the pixel-ratio multiply fails as loudly as the overflow, and Close a ≥ 48 px target above the fold — all re-asserted after a resize. Same class as R4-B: a recorded assurance whose instrument was never checked. | as above | FIX | fixed | 8a5b14b |
| R4-I | S3 | ui/QrView | §6.1 | **The QR overlay's Close button falls below the fold whenever the viewport's shorter side is its height** — a phone in landscape, or a short desktop window. `fitSize()` takes 74% of the shorter side and leaves ~150 px of non-shrinking chrome (brightness hint, grouped VIN, gaps, padding, a 56 px button) in the remaining ~26%. Measured after R4-H: galaxy-s9 landscape Close bottom 366 on a 320 fold; pixel-7 landscape 446 on 412; desktop 1024×576 588.5 on 576. The code itself stays whole and scannable and the dialog scrolls, so this is not R4-H — but `QrView`'s own comment claims the size is chosen to "fit the short side of a phone in landscape with the caption still on screen", and that claim does not hold. | measured on all three profiles | FIX | fixed | 70b7fcf |
| R4-J | S4 | ui/QrView | §9-S3 | The draw effect reads `devicePixelRatio` only when it runs, and its deps are `[value, size]`. Moving a window between a Retina and a 1× display changes the ratio without changing `size`, so the backing store keeps the old ratio and the code goes soft. Browser zoom is safe — it moves CSS-px `innerWidth`, so `size` changes and it redraws. There is no `matchMedia("(resolution: …)")` listener. | reasoned from the deps; not reproduced | FIX | open | — |
| R4-C | S2 | scan/ui | §6.1, §6.6, WCAG 2.4.11 | **The scan guide box was invisible in the light theme, and Z4 undersold it.** Z4 measured three video levels; sweeping all 256 found `--accent` light reaching **1.00:1 at video 180** — not weak, absent. The aim box is the only element in the app over arbitrary live video, so no palette colour can be guaranteed against it. Replaced with a two-tone stroke (white core, black inset ring) that takes no colour from the palette: floor **≥ 4.76:1 against the scrimmed surround and ≥ 4.61:1 across the inner edge, identical in both themes**, against WCAG's 3:1 for a non-text graphic. | `src/features/scan/CameraView.test.ts` — "draws the guide as a light stroke and a dark stroke", "takes no colour from the theme palette" | FIX | fixed | 1e5205d |
| R4-D | S2 | scan/wiring | §9-S1 | **R3-B was built but never reached the user.** `useScanner` returned `focus`, `CameraView` accepted it — as an *optional* prop with a `NO_FOCUS` default — and `ScanScreen`, the only caller, passed neither, so the tap target was dead on every platform. The optional prop was the bug: the platform gate belongs to `useScanner`, which already reports `available: false` for §11's "otherwise nothing", and that is not the same fact as a caller forgetting the prop. Now required, pinned by a `@ts-expect-error` in the test so making it optional again reddens the §13.5 gate. | `src/features/scan/CameraView.test.ts`, compile-time | FIX | fixed | — |
| R4-E | S3 | scan/ui | §6.1, §6.6, WCAG 2.4.11 | **The focus indicator inside the camera preview fails 3:1 in *both* themes.** `focus-visible:outline-accent` on the R3-B tap target sits over the scrimmed video: worst 2.50:1 dark, **1.00:1 at video 180** light. §6.6 requires a visible focus ring on the wide layout, where a desktop webcam makes it Tab-reachable. Same defect as R4-C, one element over; found by the R4-C fixer, which correctly declined to bundle a third change unasked. | measured over all 256 video levels | FIX | fixed | 1e5205d |
| R4-F | S1 | bench | §13.4, §13.6 crit. 4 | **A false accept, revealed by Z5's tier change rather than caused by it.** `code_128` severe, `EH8U2YHX60HU8VGWD` read as `EH8U2YHX60HU7VAWD`. It is a genuine Code 128 checksum collision and the arithmetic is exact: subset-B weighted deltas `−1×13 + −6×15 = −103 ≡ 0 (mod 103)`, both check characters 64 — Code 128's own check *cannot* catch that pair. Under the old tier the same VIN at the same seed decoded to `null`, so the tier was not merely uninformative at 0%, it was **masking** the one number §13.6 cares most about. §4.3 catches it downstream (`checkDigitValid` false, expected 9 got 6), so §6.3 routes it to the check-digit hold rather than a silent save. Rate 1 in 3000 severe attempts; two further full severe runs at other seeds gave zero. | printed in `bench/report.md`; `degrade(await renderBarcode("EH8U2YHX60HU8VGWD", "code_128"), "severe", 0xc5d3691c)` | **NEEDS-ZACH** | open | — |

### The two decisions R4 leaves with Zach

**R4-A — amend §4.11, or accept 1.34%?** The residue is misread North-American VINs beside a stray character. It cannot be closed by any rule expressed over run geometry and check-digit testability, because §4.11 requires the one shape and forbids the other, and they are the same shape. Either §4.11's 18-char row changes to `NO_VIN`, or this stays as a documented limit.

**R4-F — does §13.6 criterion 4 count a wrong read that §4.3 flags?** §13.4 defines a false accept as "a wrong VIN **confirmed**". This one is never confirmed: it lands in the §6.3 mismatch banner and needs an explicit *Use as-is*. The bench's classifier counts any extraction ≠ ground truth. Either the classifier is right and criterion 4 is genuinely violated, or it is stricter than §13.4 and this wants a separate count. **The classifier was deliberately not touched** — narrowing it in the session that made the number nonzero would be tuning to green.

### What R4 changed about how measurement is trusted

R4-B is the finding that matters most for everything above it. Every rate in this ledger produced by the house LCG — Z1's 2,000, Z6's in-suite 2,000 and 5,000, R2-F's, R3-M's 3,000 — was measured on a generator with a period of 10,466. The decisions those numbers supported still look right, and the corrected re-measurement is in flight, but the honest statement is that the samples were about a quarter of what was claimed. A floor assertion on distinct payloads is being added so this class of defect cannot silently return.



## Round 5 — mutation testing, first run (A23)

`bun run mutate` existed for the first time tonight. 2,879 mutants, 36 files, **20 min 58 s** on 4 CPUs.

**§13.5 scope (`src/lib/*`): 85.01% — the 80% threshold passes.** For contrast the coverage half is 99.36% lines / 96.72% branches on 1,157 green tests, which is the point: coverage said the tests were thorough, and mutation says where they are not.

| directory | score | survived | no-coverage | timeout |
|---|---:|---:|---:|---:|
| `src/lib/vpic` | 95.07 | 14 | 1 | 4 |
| `src/lib/vin` | 94.49 | 15 | 0 | 2 |
| `src/lib/storage` | 87.89 | 54 | 1 | 1 |
| `src/lib/payload` | 86.55 | 53 | 0 | 2 |
| `src/lib/auth` | 81.48 | 73 | 2 | 0 |
| `src/lib/sync` | **78.13** | 183 | 11 | 4 |
| **`src/lib/*`** | **85.01** | 389 | 15 | 13 |
| `scanMachine.ts` (reported separately) | 98.77 | 2 | 0 | 0 |

**The four files §13.5 gates at 100% coverage score 96.61, 90.00, 80.95 and — `codec.ts` — 76.83.** All four are at 100% lines and branches. That gap is the finding.

| id | sev | area | spec ref | description | repro | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| A23 | S3 | gate | §13.5 | `bun run mutate` did not exist and the mutation half of the gate had never run in five rounds. Stryker 10 + vitest-runner, scope mirroring `vitest.config.ts`, `break: 80` untuned. | full run, exit 0 | FIX | fixed | 900e541 |
| A23-a | S2 | tests | §5.1 | **`db.test.ts` passes while testing nothing under a thread pool.** Five of its seven tests set `process.env.TZ` at runtime, which only moves the clock under vitest's `forks` pool; Stryker's runner hard-codes `threads` for vitest ≥ 4.1.0. The file exists precisely because the offset sign and minutes "were never exercised at all" — under threads it silently reverts to that state. | `bunx vitest run src/lib/storage/db.test.ts --pool=threads` → 5 failed | FIX | open | — |
| A23-b | S3 | tests | §4.2 | `extractVin.adversary.test.ts`'s "degrading superlinearly" asserts **no timing**. The complexity claim is enforced only by vitest's ambient 5 s default, which varies with machine load. | instrumenting `checkDigit.ts` pushed it 635 ms → >5000 ms | FIX | open | — |
| A23-c | S4 | gate | §13.5 | Stryker reports a mutant that crashes module evaluation as `Survived` (`testsCompleted: 0`) rather than killed. Five false survivors in `fields.ts` understate the score by 0.16 pt. | all five have `testsCompleted === 0`; all 211 genuinely-tested static mutants have > 0 | WONTFIX | open | — |
| M1 | S2 | payload | §4.9, §6.4 | **`codec.ts` 76.83%, and 27 of 38 survivors are one habit**: the rejection tests assert `PayloadError.code` only, and three distinct guards all produce `"encoding"`. `TextDecoder({ fatal: true })` → `false` survives, so nothing pins the guard whose comment says it stops mojibake "being stored as a record". Every §6.4 message string mutates to `""` and survives. | `bun run mutate`; codec.ts:232, 242, 243 | FIX | open | — |
| M2 | S2 | vin | §4.2, R4-A, N2 | **The only survivor in this report that maps to a wrong VIN accepted.** `valid.length === 1 → true` and `run.slice(...) → run` both survive: no test has a **second run** contributing another check-digit-valid window, so §4.2's ambiguity refusal is unpinned. Both mutants show one VIN as fact where the original refuses. | needs a payload with two runs — one a clean VIN, one longer and holding a different valid window | FIX | open | — |
| M3 | S2 | storage | §5.5 | `decodeQueue.ts` — `isVisible()` has 10 survivors (body emptied, forced true, forced false, inverted, `||`→`&&`) and the interval kicker is entirely unpinned. `!isOnline() → false` survives on the decode pump. | `bun run mutate`; re-measure, the file moved during the run | FIX | open | — |
| M4 | S2 | sync | §13.5 | `src/lib/sync` 78.13%, under the bar: `pull.ts` 63.89, `engine.ts` 74.29, and 11 mutants with no coverage at all. S4 code whose tests were being written during the run. | per-directory table | FIX | open | — |
| M5 | S3 | storage | §4.12, D11 | `upsert.ts` — `firstScannedAt <` → `<=` and `lastScannedAt >` → `>=` survive (no equal-timestamp test), and `unit !== … \|\| notes !== …` → `&&` survives (no test changes exactly one field). | upsert.ts:93, 109, 111 | FIX | open | — |
| M6 | S3 | storage | §13.2 | Corrupt-row guards survive **as a class** — `normalize.ts:24`, `outbox.ts:134`, `db.ts:66`. The tests assert the record comes back sane, never that *this guard* is what made it sane. The `db.ts` one also leaves §5.7's client-UUID crypto fallback never taken. | `bun run mutate` | FIX | open | — |
| M7 | S3 | vpic | §4.7 | `client.ts` — `results.length === 0 → false` survives (§13.2's empty-`Results` case not distinguished); `finally { clearTimeout }` and `catch` bodies removable; `"Unknown error"` has no coverage. | client.ts:89, 104, 109, 134 | FIX | open | — |
| M8 | S3 | payload | §4.9 | `carrier.ts` — the anchor drops off `/^VINRELAY\d+:/i` and survives, so `"junkVINRELAY1:BODY"` becomes a carrier and the body is sliced from the wrong offset. The doc says a mangled carrier "must never reach `extractVin`". | carrier.ts:34, 73 | FIX | open | — |
| M9 | S3 | vin | §4.2 | `if (valid.length > 1) return null;` in `extractVin` is **redundant** — that condition implies `candidates.length >= 2`, so the fall-through returns `null` identically. 100% branch coverage covers a line nothing can distinguish. A source finding, not a test gap. | extractVin.ts:79 | FIX | open | — |
| M10 | S3 | scan | §6.3 | `scanMachine.ts:184 hiddenAtMs === null → false` survives: a `visible` with no preceding `hidden` and a non-idle state is untested. The file's other survivor is provably equivalent, so it is really 161/162. | `bun run mutate` | FIX | open | — |
| M11 | S2 | ui | §6.1, §6.4, N2, §13.5 | **26 production files have no unit test at all, so no mutation score.** `vitest.config.ts` is `environment: "node"` and there are zero `*.test.tsx` in the repo. That is the whole of §6.1's field-usable UI, §6.4's rendered error states, N2 at the render layer and the ZXing seam. Their only cover is Playwright, which mutation testing does not reach — so the `width > 150` guard that could never fail is exactly the class of defect this gate still cannot see. | `grep -c 'test.tsx'` → 0 | NEEDS-ZACH | open | — |

**`mutate` should stay per round, not per fixer commit.** Measured: typecheck ~5 s, lint ~10 s, test ~15 s, coverage ~15 s, **mutate 20 min 58 s** — a 60–80× tax on every commit. §13.5 currently says "every round, and every fixer commit"; the first is affordable, the second would stall the loop.

The cost is concentrated and should not be optimised away: **506 static mutants, 18% of the total, consume 87% of the runtime**, because each forces a module reload. `ignoreStatic: true` would cut the run to ~3 minutes and is exactly the wrong economy — the static mutants **are** the §4 constants. `wmi.ts` and `symbologies.ts` at 100%, `modelYear.ts` at 96.61% and `fields.ts` at 99.5% are scored almost entirely on them, and that is the strongest evidence in this report that the §4.11 fixtures do their job.


## Round 5 (bench): the instrument, and what it was worth (B2)

`bun run bench` now decodes **in Chromium, through the app's own reader**. `bench/browser-entry.ts` is bundled from the working tree on every run and evaluated in a page: `new BrowserMultiFormatReader(buildScanHints())` — the §4.6 function `readScanResult` builds its reader from, imported and not copied — and `decodeFromCanvas`, which is the exact call `BrowserCodeReader.prototype.scan` makes on every frame. `bench/browser-decode.ts` drives one browser with a four-page pool and batches frames over CDP; a page per frame would have been hours, one page for 4,200 frames is minutes. The node `RGBLuminanceSource` path is kept and renamed `rgb`, as the control.

Every frame is degraded **once** and offered to each instrument, so the delta is measured on identical pixels rather than on two runs that merely shared a seed.

### The delta, which is the headline

Full corpus, 200 VINs, seed `0x5eed1a7c`, 4,200 frames per path, 12,600 attempts, **5 min 06 s** wall on four cores.

| | correct of 4,200 | read only by this path | frames whose decoded text differed |
|---|---:|---:|---:|
| `canvas` (the app) | 2,839 | — | — |
| `rgb` (the old instrument) | 2,839 | 0 | **0** |
| `yuv` (`canvas` + a modelled I420 round trip) | 2,838 | 15 (and 15 lost) | 44 |

**Against `rgb` the delta is exactly zero, in every one of the 21 cells, on every one of the 4,200 frames** — not merely the same rate, the same bytes out of the decoder. So B2's premise does not reproduce at scale, and the honest reading is that the bench was measuring the right program for the wrong reason. The mechanism: this corpus renders grey, and on a grey frame the two luminance sources are the same function — `(r + 2g + b) / 4` and `(306r + 601g + 117b + 512) >> 10` are both exactly `v` at `r = g = b = v`. What is left between them cannot bite here either: `isRotateSupported()` is true only on the canvas source, which earns `OneDReader` a 90°-rotated retry under `TRY_HARDER`, and rotating an already-horizontal symbol into a vertical one helps nothing; and `decodeWithState` versus `decode(bitmap, hints)` rebuild the same readers from the same hints. The `yuv` column is what says the delta table can see a difference at all — it moves 44 frames, in both directions, and it is the only reason an all-zero `rgb` column can be read as a result instead of as a harness that forgot to switch instruments.

The one anecdote B2 rested on — a §4.9 payload QR the harness missed and the browser read — is therefore **not** a luminance-source effect. The likeliest explanation is the one the bench cannot reproduce by construction: the app gets a new frame every ~100 ms and needs two agreeing reads, while the bench gets exactly one attempt per image. A single-shot rate is a floor on a multi-frame scanner, not an estimate of it.

### R4-F survives the instrument change

The false accept is not an artifact of the node path. `code_128` severe, `EH8U2YHX60HU8VGWD` → `EH8U2YHX60HU7VAWD`, seed `0xc5d3691c`, reproduces identically on **all three** paths, including the app's. §13.6 criterion 4 is still 1, and R4-F's decision is still Zach's.

### What is still not a camera (B2a)

`bench/camera-probe.ts` answers the part of B2 that was never about the luminance source. It pads a subset onto one 1920×1080 white field, stamps a frame index into the corner so a decode is attributable, writes `C420` YUV4MPEG2 in the same shape `make-fake-camera.py` does, and hands it to Chromium's `--use-file-for-fake-video-capture` — so the frame reaches `drawImage(video)` through I420 exactly as a camera's would. Each frame is then decoded twice: off the video, and off the identical padded PNG.

105 frames (5 VINs × 7 symbologies × 3 tiers, the same seeds as above), video track reported 1920×1080 so nothing was rescaled:

| source | correct of 105 | false accepts |
|---|---:|---:|
| camera (fake device → `<video>` → canvas) | **67** | 0 |
| canvas (the padded PNG → canvas) | **69** | 0 |

Two frames, both `code_39_i`, the same two on every run. The camera reads **worse**, not better, and deterministically so: a `C420` stream with no `XCOLORRANGE` is expanded as studio swing, which clips at both ends and requantises. That is the direction that matters — it means the bench's canvas numbers are a *ceiling* on the capture path, by about 2 points on this subset, not a floor.

It is a probe and not the bench because the whole corpus through the camera is a 12.4 GB y4m and roughly three hours of playback at a frame rate slow enough for every frame to be decoded before the camera moves on, and because padding every frame to one resolution is a degradation §13.4 does not define. `getUserMedia` also needs a secure context, so the probe serves its blank page from `http://127.0.0.1` on an **ephemeral port** (`listen(0)`) rather than picking one that could collide with a preview server or another agent's Playwright run.

### What the report now says about itself

The run header names the decode path and the binary; the rates, miss reasons and false accepts are the app path's unless a row says otherwise; the off-path false accepts are listed rather than dropped; and the count of reads carrying a §4.6 AIM identifier is printed, which on this corpus is 0 — no rendered row carries a *leading* FNC1, so the `]C1` strip is inert here and no rate depends on it.

**Time-to-confirm is still not measured.** §13.4 asks for it beside the decode rates; confirmation is two agreeing reads inside §6.3's window, which is run (b)'s job, and run (b) is the Playwright fake-camera suite rather than this bench. The report now says so in the timing section instead of leaving the omission to be noticed.


## Compatibility: what "works on every phone" actually resolves to

Asked to make this work on every phone from the past fifteen years. It cannot, and the binding constraint is not application code — it is the stack §2 locks. Measured rather than recalled, on a worktree at `1e5205d`.

**The JavaScript is not the problem.** The same tree builds clean at `safari16.4`, `safari15`, `safari14` and `chrome90`; the bundle moves 990 kB → 994 kB across all four. Lowering the build target is essentially free.

**The CSS is the floor, and it does not move.** The emitted stylesheet is byte-identical at every one of those targets: 37 `@property` rules and 2 `color-mix()` calls, both Tailwind v4 output, neither lowerable by esbuild or any target setting.

| feature | emitted | support floor | what happens below it |
|---|---|---|---|
| `@layer` | 5 | Chrome 99 / Safari 15.4 (Mar 2022) | the **entire** Tailwind stylesheet is dropped — the app renders unstyled |
| `@property` | 37 | Chrome 85 / Safari 16.4 (Mar 2023) | shadows, rings, transforms and filters silently stop rendering |
| `color-mix()` | 2 | — | **not a floor.** Already wrapped in `@supports (color:color-mix(in lab, red, red))` and used only for `::placeholder`, falling back to `currentColor` |

So the honest floor is **Chrome 99 (March 2022) and Safari 16.4 (March 2023)** — not the Chrome 111 first reported, which came from reading Vite's baseline rather than the emitted CSS. About four and a half years on Android, three and a half on iOS.

Independent of the stack, a camera-scanning PWA has physical floors: `getUserMedia` reached iOS Safari in **11** (2017) and service workers in **11.3** (2018). A 2011 phone has neither, nor usable IndexedDB.

**The one lever that moves this materially is a §2 stack change** — Tailwind v4 → v3, or a PostCSS pass emitting static fallbacks. It is a locked-stack change and therefore Zach's. NEEDS-ZACH.

### What was done instead, and what it measured

**Graceful degradation, verified by removing one capability at a time** from the built app on a Pixel 7 profile, across all five routes:

| removed | renders | manual save |
|---|---|---|
| `mediaDevices` · `crypto.randomUUID` · all of `crypto` · `serviceWorker` · `vibrate` · Web Share · `clipboard` · `matchMedia` · `AudioContext` | ok | **SAVED** |
| `indexedDB` | ok | cannot save — **and says so**: *"Couldn't save this VIN — Nothing was written. Your entry is still here — try again."* |

No blank route and no page error in any scenario. The one case that cannot save reports it, which is P7 working rather than P7 promised.

**Layout across eight Android profiles**, Galaxy S III (2012, 360 px) through Galaxy S9+ (320 px) to Pixel 10 Pro XL (448 px): **targets under 48 px: 0. Horizontal scroll: none.** `VinDisplay` measures **28 px with 2.24 px letter-spacing** on all of them, meeting §6.1's floor on the narrowest profile available.

**The whole e2e suite now runs under two emulated Android phones** (`37f4318`): 64 of 64, fake-camera scan flows included. It emulates the surface and not the engine, so it does not close §7 item 4 — but Android Chrome had never been in the gate at all before.

**It paid for itself within the hour.** R4-H — the handoff QR unscannable on every phone — is a DPR bug, and DPR is exactly what a desktop-only gate cannot see. So is R4-H', the guard that was supposed to be watching it and could not fail. Neither is subtle; both were simply invisible to a suite that only ever ran at devicePixelRatio 1. That is the argument for keeping the Android projects in the gate rather than treating them as a one-off audit.

## Round 6 — the deployment round: three defects that only a real deploy could surface

Not a scheduled §13.3 round. These came out of building two deployment targets — a single-file
demo and a GitHub Pages `/docs` build — and every one of them was invisible to the existing gate
because the gate only ever built and served the app at the root of a real origin.

| id | sev | area | spec ref | description | repro / test | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| F1 | S1 | storage/rendering | §6.4, N1, P1 | **The whole app rendered a blank page when IndexedDB was unavailable.** There was no React error boundary anywhere in `src/`, and `dexie-react-hooks` re-throws a rejected live query *during render*, by design, so a boundary can catch it. `Shell` wraps every route and called `useLiveQuery` itself, so the throw unmounted the root: no Scan screen, no banner, no way to type a VIN. The write path was already defended; only the read path had no floor. Fixed with a boundary at three depths, smallest subtree first, keyed on `pathname` so a screen that threw does not leave its notice standing over the next one. | `tests/e2e/storage-unavailable.spec.ts` — pre-fix, `#root` is **0 bytes** with `IDBObjectStore` reads throwing `UnknownError` and stays 0 after navigating anywhere | FIX | fixed | `4599f39`, `1933ecc` |
| F1-a | S3 | ui | §6.1, §13.5 | The boundary's Reload target **measured 48 px while the commit and its test both claimed 56 px**, and the guard could not detect it: `toContain("min-h-[var(--tap-lg)]")` checks a class token in static markup, so it passed at 48 px. `Banner`'s `[&>*]:min-h-[var(--tap)]` beats the primary variant's own min-height. Same dead-guard class as R4-H'. Fixed with the established `h-14` pin and an assertion that measures the rendered box. | `tests/e2e/storage-unavailable.spec.ts:155` — goes red at `Expected: >= 56 / Received: 48` with the pin removed | FIX | fixed | `1933ecc` |
| F1-b | S2 | storage/rendering | §6.4, P7 | **The boundary cannot catch the case where IndexedDB never opens at all.** Dexie's `liveQuery` filters `DatabaseClosedError` and `AbortError` before calling `observer.error`, so the observable never emits: the hook returns `undefined` forever, nothing throws, and History renders its heading and then nothing — no rows, no "Nothing scanned yet", no error, permanently. Not a crash and not a blank app (Scan and manual entry both work), but a missing error state. Needs a storage-availability probe, which is a different change from F1. | the two `[F1] the keyboard path survives when …` cases leave the gap visible rather than asserting it away | FIX | open | — |
| F2 | S2 | sheet/payload | §4.9 | **The handoff QR encoded the site root, not the app, under a sub-path deployment.** `Actions.tsx` passed `window.location.origin` — the bare origin, no path. Decoded off the rendered canvas while the page sat at `/vinreader/`: `http://127.0.0.1:4173/#/i?d=…`. On Pages that is the user's root site, so scanning from a second phone 404s. `codec.ts` `baseUrl()` was already correct (§4.9, authoritative, untouched); only the caller was wrong. Fixed by resolving `import.meta.env.BASE_URL` against `location.href` — resolution, not concatenation, because the demo's base is `./`. | `src/app/appBase.test.ts` (7); root case pinned byte-identical to the old argument through `buildPayloadUrl`, so root deployments move nothing | FIX | fixed | `a63d55a` |
| F2-a | S4 | tests | §13.5 | **The changed line is unpinned.** Reverting `Actions.tsx` to `window.location.origin` leaves the whole suite green: vitest runs in `node` with no DOM, and the Playwright harness only ever serves the root build, where the two arguments are byte-identical by construction. The derivation is well tested; the wiring is not. Closing it needs a Playwright project that serves `docs/` under `/vinreader/` and decodes the QR canvas — new gate infrastructure, correctly outside the smallest fix. | — | FIX | open | — |
| F3 | S3 | build/css | §7 item 5 | **The stylesheet was being built out of its own output.** Tailwind v4 source detection scans the filesystem, not git, so `docs/` — intentionally not gitignored, because Pages serves from it — fed built HTML back in. The emitted CSS carried `.italic` and `.invert` with zero uses anywhere in `src/`. Fixed with `@source not` for all four build directories, named explicitly so the CSS stops depending on an unrelated tool's ignore file. | `src/index.sources.test.ts` runs the real pipeline and plants a probe in each directory; polluted 21,858 B vs clean **21,451 B**, md5 `732376a…`, delta 407 B | FIX | fixed | `fc10e21`, `1933ecc` |
| F3-a | S4 | build/css | §7 item 5 | The same leak survives from non-build files: the whole-repo scan emits 213 utility rules against 211 for `src/` plus the root `index.html`. The two extras are `.transform` and `.transition`, extracted from the bare words in `hardening/HARDENING_S1.md` and `VIN_RELAY_BOOTSTRAP.md` — **the ledger is polluting the stylesheet it audits.** Test files under `src/` do it too; that is how the first version of F3's own test failed. Fixed with one `@source not "../**/*.md"`. The positively-scoped alternative was built and measured too — it emits 182 rules, dropping `.transform` and `.transition`, but it moves the stylesheet off the F3 baseline and makes every file outside `src/` a non-source with nothing to notice one going missing; the root `index.html` having to be handed back by name is that failure on day one. The fix this row proposed does not work as written: `source("./src")` resolves against the CSS file's own directory and errors as "does not exist or is not a directory". | `src/index.sources.test.ts` plants a probe in a root `.md` and in this ledger; with the directive deleted it fails twice, the second off the real committed ledger | FIX | fixed | `fa9e3a0` |
| F4 | S3 | ui/Banner | §6.1 | **The primitive contract is papered over, not fixed.** Inside a `Banner` action row a `<Button variant="primary">` computes `min-height: 48px` even with F1-a's pin, so `Button.tsx`'s documented "primary gets the 56 px target" is false wherever a Banner is the parent. Six call sites now remember `className="h-14"`; a seventh will silently render 48 with nothing to catch it. | measured in Chromium at 390×844 with the pin in place | FIX | fixed | 2632c6b |
| F5 | S2 | import | §4.9 | **An imported payload's summary fields are never persisted.** `runImport` passes only vin/origin/symbology/raw/checkDigitValid/at/deviceLabel/unit/notes to `upsertVehicle`, so `payload.mk/md/tr/bc/en/fu/dr/gv` reach the preview and die there — while §4.9 says the summary fields are used immediately so the receiver is useful offline too. Import `{"v":1,"vin":"1HGCM82633A004352","y":"2003","mk":"HONDA","md":"Accord"}` offline, then decode the sheet's QR: HONDA and Accord are gone. | decode the re-encoded payload after an offline import | FIX | open | — |
| F6 | S3 | sync/storage | §4.12, §13.5 | **A pre-existing flaky test, and the gate is what it costs.** `scenarios.test.ts > a delete followed by a re-scan > leaves the record alive on both sides` fails intermittently. `merge.ts:134` resurrects a deleted record only on a *strictly* newer scan — `newer(pending.scanAt, remote.deletedAt)` — and `nowIso()` stamps from `new Date()`, so a soft delete and the re-scan after it can land in the same millisecond, the tie falls to the delete, and the re-scanned truck stays deleted. **Field impact is near nil**: no human deletes and re-scans inside 1 ms. The real cost is a §13.5 gate that goes red at random, which is a gate you stop reading. Measured over 8 full-suite runs per tree: HEAD 2/8 red (this test once, `a pull that arrives mid-push` once), at `fa9e3a0` 1/8 red. A monotonic `nowIso` in the storage layer would fix it without touching §4.12; relaxing `newer` to a non-strict compare would be a §4.12 merge-rule change and is **NEEDS-ZACH**. | `bun run test` repeatedly; `deleted_at: "…T12:00:00.005Z"` where `null` is expected | FIX | open | — |

### What this round says about the gate

Every one of F1, F2 and F3 was reachable only by *deploying* the app somewhere other than the
root of a real origin. 1,261 unit tests, 42 e2e and a 99%/97% coverage floor did not see a blank
screen on storage failure, a handoff QR pointing at the wrong site, or a stylesheet built from its
own output — because none of them ever left the happy origin.

F2-a is the sharpest version of this: the fix is well tested and the *wiring* is not, and the
reason is the same reason the defect existed. A gate that only serves the root build cannot tell
`window.location.origin` from the app base, so it cannot fail on the line that mattered.

**Two things the round did not do.** The bench is still red on R4-F (a genuine Code 128 checksum
collision, `EH8U2YHX60HU8VGWD` → `EH8U2YHX60HU7VAWD`) and on the §13.6 threshold cells blocked by
B1/B3 — decode counts bit-identical to the tracked report, so nothing here moved them. And F1's
five microcopy strings are supplied under §0 rule 4, not §6.4, and are pinned verbatim by
`src/app/ErrorBoundary.test.ts` pending Zach's sign-off.

## Round 7 — `harden S1`, round 1 (§13.3)

The first scheduled §13.3 round on this slice. Five auditors in parallel against a ledger already
carrying 35 open rows, briefed to find what is NEW and what is STALE rather than restate it.

**36 findings, 32 of them new: 3 S1, 9 S2, 16 S3, 8 S4.** Buckets: 32 FIX, 3 NEEDS-ZACH, 1 WONTFIX.

| id | sev | area | spec ref | description | repro / test | bucket | status | commit |
|---|---|---|---|---|---|---|---|---|
| G1 | S1 | scan/extraction | §4.2 steps 1 and 2, §4.1, N2, §13.6 crit. 4 | §4.2 step 1's "Uppercase" is a LENGTH-CHANGING map, and it runs before step 2 splits the string into runs. Fifteen code points outside the §4.1 alphabet uppercase INTO it, six of them into two or three characters: U+00DF ß→SS, U+FB00 ﬀ→FF, U+FB02 ﬂ→FL, U+FB04 ﬄ→FFL, U+FB05 ﬅ→ST, U+FB06 ﬆ→ST, plus U+017F ſ→S and eight partials (ﬁ→F+I, ẖ→H+◌̱, ǰ→J+◌̌, …). A character step 2 would treat as a SEPARATOR is therefore turned into alphabet characters before step 2 ever sees it — lengthening the run it sits in and joining two runs into one. This defeats R4-A's whole-run rule, which is the only thing standing between §4.2 and a fabricated VIN, because it is stated over run geometry: extractVin("1HGCM82653A0ß352") — 16 characters, runs of 12 and 3, no 17-character window at any offset — returns { vin: "1HGCM82653A0SS352", checkDigitValid: TRUE }. A VIN on no label, as fact, with the success beep and "Got it ✓", and §6.3's two-read rule agrees because a 2D code decodes identically every frame. Distinct from the prefix cases already pinned in extractVin.adversary.test.ts (`ſ${VIN}`, `ﬅ${VIN}`): those grow an 18/19-character run in which the VIN is not the whole run, so R4-A refuses them. Interior expansion manufactures a whole run and walks straight through. Reachable: @zxing/library's Data Matrix parser emits String.fromCharCode(value+128) for an Upper Shift byte (DecodedBitStreamParser.js:93-95, 202), so label byte 0xDF decodes as ß, and QR byte segments default to ISO-8859-1; useScanner.readScanResult hands that text straight to extractVin. The paste/import path takes it too. NEEDS-ZACH because the two halves of §4.2 contradict each other on this input — step 1 says uppercase, step 2 says "any other character is a separator" — and resolving it means writing which wins. The smallest reading that keeps both true is to uppercase only within ASCII (a-z→A-Z), which changes nothing for any §4.1 or typed input. | bun run test -- src/lib/vin/extractVin.unicodeCase.adversary.test.ts — 4 RED. extractVin("1HGCM82653A0ß352") → {vin:"1HGCM82653A0SS352", checkDigitValid:true}; extractVin("8CJ7BLMR2AWTß9K4") → {vin:"8CJ7BLMR2AWTSS9K4"} where splitRuns of the ASCII-uppercased raw is ["8CJ7BLMR2AWT","9K4"]. src/lib/vin/extractVin.ts:17 | FIX | **fixed** | 8a7e1a1 — one `asciiUpper` in grammar.ts, imported by extractVin, ManualEntry, SheetScreen and shareText (a fourth normaliser the audit had not found). 4 RED green; grammar.test.ts sweeps all 1,111,998 code points both ways; smoke.spec.ts covers the typed field and the route parameter |
| G2 | S1 | scan/carrier, import/paste | §4.9, D14, N2, §13.6 crit. 4 | carrier.ts states the requirement in its own words — "a carrier mangled in transit is no longer decodable, but it still must never reach extractVin" — and does not meet it. TEXT_CARRIER_RE only tolerates a damaged version digit or case; URL_CARRIER_RE needs a literal '#'. Two things that routinely happen to a link between two phones defeat both: percent-escaping (# → %23, what a redirector or link shortener does — R3-M measured this exact shape and closed only the text-prefix half) and fragment stripping (a chat client keeps ?d=… and drops #/i). matchCarrier returns null, so ImportScreen.readPaste falls through parseCarrier → parseShareTextVin → extractVin(raw) (ImportScreen.tsx:338-371) and §4.2 mines the base64url body; useScanner.readScanResult has the same fall-through for a re-printed QR. The "belt and braces" argument in carrier.adversary.test.ts — that §4.2 refuses base64url bodies — is wrong, and it rests on one hand-picked body and on ledger Z6's per-run rule that R4-A explicitly deleted. A base64url body splits into ~30 short runs on '-', '_' and (after step 1 uppercases it) 'I', 'O', 'Q'; whenever exactly one run is exactly 17 characters, step 4(b) returns it. Measured over 20,000 seeded payloads (mulberry32 seed 20260904, per R4-B): 633 fabricated a VIN = 3.17% [2.93%, 3.42%], 15 of them check-digit VALID, and 576 = 2.88% [2.66%, 3.12%] are written with NO banner at all, because a fabricated window lands a letter in position 9 nine times in ten, so §4.3 checkDigitApplies is false and D03's gate in useVinCommit.request never fires. The sheet then prints §6.4's reassuring "This number doesn't use a check digit." over a number no label carries. Not a §4 change: R3-M's own precedent is that widening the recogniser leaves §4.9 untouched. | bun run test -- src/lib/payload/carrierMangled.adversary.test.ts — 5 RED. Literal: the §4.11 fixture handoff for 1HGCM82633A004352 / UNIT-7 / "Yard truck 12", sent as https://vinrelay.example/%23/i%3Fd%3DeyJ2IjoxLCJ2aW4iOiIxSEdDTTgyNjMzQTAwNDM1MiIs… → extractVin returns WWFYZCB0CNVJAYAXM, checkDigitApplies false. src/lib/payload/carrier.ts:34,55 | FIX | open | — |
| SB-1 | S1 | bench/scanner | §13.4, §13.6 crit. 4, §13.3 | A SECOND false accept, distinct from R4-F, in a different symbology row at a different seed. Sweeping four further full-corpus canvas runs (21,000 attempts total across five seeds) turned up `KB7BWYDJ6TW0808Z3` read as `KB7BWYDJ6TW0874Z3` on code_128_fnc1/severe (drawn extras warp+low_light). The decoded text is `KB7BWYDJ6TW0874Z3\x1dzP84203908`: BOTH fields are corrupted — the VIN's `0808` became `0874` and the MH10.8.2 field `1P84203911` became `zP84203908` — and Code 128's mod-103 check is still satisfied, so the symbology's own check cannot catch it. ASSUME_GS1 worked (the GS survived), §4.2 split correctly, and step 4(b) returned the 17-character run with `checkDigitValid: false`, so §4.3 routes it to §6.3's check-digit hold rather than a silent save — the same downstream mitigation, and the same N2 exposure through 'Use as-is', as R4-F. This matters for R4-F's pending decision in two ways: its row asserts 'two further full severe runs at other seeds gave zero', which no longer holds at a wider sample, and the phenomenon is not confined to one VIN or one Code 128 row. Measured rate across all five seeds: 2 / 21,000 = 0.0095%. Filed NEEDS-ZACH because it is the same Code 128 checksum-collision class R4-F already escalated and every mitigation touches §4.6 hints or §4.2. | `bun run bench/run.ts --paths canvas --seed 0x7f3ac91d` -> 'false accepts: 1 on canvas (threshold 0) BLOCKER'. Standalone, verified: `degrade(await renderBarcode("KB7BWYDJ6TW0808Z3", "code_128_fnc1"), "severe", 0x55f2df0a)` decoded on the canvas path -> text `KB7BWYDJ6TW0874Z3\x1dzP84203908`, extractVin -> `KB7BWYDJ6TW0874Z3`, checkDigitValid false. `severeExtrasFor(0x55f2df0a)` = warp+low_light. | NEEDS-ZACH | open | — |
| F1-b | S2 | storage/rendering | §6.4, P7 | Reproduced, and it is worse on one route than the row records. With indexedDB.open throwing, /#/v/:vin renders an entirely EMPTY <main> — main.innerHTML.length is 0, no heading, no message, no error — because SheetScreen.tsx:256 is `if (record === undefined) return null` and the live query never emits. F1-b describes History as "renders its heading and then nothing"; the Sheet renders nothing at all, and the Sheet is where a scan lands. Two other routes were checked under the same fault and are fine: Account renders its "Sign-in isn't set up in this build" panel, and Import renders its preview normally (the write then fails loudly). Evidence extending the existing row, not a new one — same cause, same fix (a storage-availability probe). G5 above is the separable part of it. | page.addInitScript(() => { indexedDB.open = () => { throw new DOMException('denied','SecurityError'); } }); page.goto('/#/v/1HGCM82633A004352') → await page.locator('main').innerHTML() is '' (length 0); body text is just the nav, "Scan History Settings". | FIX | open | — |
| F7 | S2 | scan/layout | §6.1 (targets ≥48/≥56), §6.4 ("Under a notice that can be retried: Retry, then Type VIN instead"), P7 | On the two camera-error states the §6.4 escape routes are pushed out of the scroll view and are not visible at all on the two Android profiles the project's own gate runs. CameraView only hides the preview in `confirmed` (CameraView.tsx:233-238: `state.kind === "confirmed" ? "hidden" : ""`), so in `error` the dead `aspect-[3/4] max-h-[60vh] flex-1` black box still eats the fold and pushes the notice, Retry and "Type VIN instead" below it. Measured visible height against `main`'s client rect, plus `document.elementFromPoint` at each button's centre. Galaxy S9+ (320×658, the profile `bun run test:e2e:android` uses): permission_denied Retry 0/56 px, Type VIN instead 0/48 px; no_camera the same, both 0 px; hit test returns the bottom nav "History" for both. Pixel 7 (412×839): Type VIN instead 0 px in both states, Retry 25.11 px in permission_denied. 390×844: Type VIN instead 0 px (permission_denied) / 13.34 px (no_camera). 360×640: both 0 px in both states. The banner itself is clipped too (83.7 of 146.5 px at 320). `main` does scroll (scrollHeight 836 vs clientHeight 609) but nothing on screen says so, and the whole visible area is a black rectangle plus part of a warning. Same mechanism R3-F1 records for the carrier banner in `streaming` — this is the `error` branch, which no row covers, and a fix aimed only at the carrier banner will not close it. Existing cover cannot fail: CameraView.test.ts:319 asserts the two button labels in static markup, and scan-double-tap.spec.ts clicks the button, which Playwright scrolls into view first. | npx playwright test --config /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/pw.config.ts --project=phone fold2 (also `fold` for the plain viewports). Stubs `navigator.mediaDevices.getUserMedia` to reject NotAllowedError for permission_denied; no_camera needs no stub in headless Chromium. | FIX | open | — |
| F8 | S2 | sheet/layout | §6.1 (targets), §6.4 (Sheet — Delete: "Delete this vehicle?" → Delete / Cancel, "with the VIN shown so the wrong row cannot be deleted from the bottom of a long sheet") | The armed delete confirmation renders below the fold, and the tap that misses it navigates away. Delete is the last control on a ~1955 px sheet, so the user must be scrolled to the bottom to reach it; arming it appends the confirmation banner *below* that point and nothing scrolls it into view. Measured after setting `main.scrollTop = main.scrollHeight` and clicking Delete via `element.click()` (no Playwright auto-scroll): 390×844 — Delete 1.75/48 px visible, Cancel 1.75/48, banner 88 of 152 px; Galaxy S9+ — both 0/48 px, banner 88 of 174; Pixel 7 — both 2.25/48. `document.elementFromPoint` at the two centres returns the bottom nav's "Scan" and "History" links, so the confirming second tap of a two-tap destructive flow lands on navigation instead. The user sees the question ("Delete this vehicle?" + the VIN) and not the answer. Same class as A08 (fixed for the scan mismatch) at a site no row names. | npx playwright test --config /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/pw.config.ts --project=phone delete2 — prints box/visible/hit for Delete and Cancel on 390×844, Galaxy S9+ and Pixel 7. | FIX | open | — |
| G5 | S2 | settings/rendering | §6.4, P7 | Settings holds the reason it is empty and renders "Loading…" instead of it, permanently. SettingsScreen already runs the storage-availability probe F1-b says is missing — useEffect(() => { getSettings().catch((cause) => setError(describe(cause))) }) at SettingsScreen.tsx:229-231 — so the failure IS captured, into `error`. But the loading gate twelve lines below, `if (!stored) return <h1>Settings</h1><p>Loading…</p>` at SettingsScreen.tsx:243-251, returns before the render that would show `error`. The live query never emits (Dexie filters DatabaseClosedError out of liveQuery), so `stored` stays undefined and the screen says "Loading…" for the rest of the session while holding the sentence that explains it. Settings is also where "Clear all data" lives — the only recovery a user has for a wedged database — so the screen that cannot say what is wrong is the screen they are sent to. This is NOT F1-b: F1-b needs a probe that does not exist; this one has the probe and steps over its answer, and the fix is to render `error` above the loading line. | CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome npx playwright test --project=desktop tests/e2e/storage-never-opens.spec.ts — 2 RED (indexedDB.open throws, and indexedDB absent). "Loading…" resolves to 1 element for the full 10 s timeout on both faults. | FIX | open | — |
| G6 | S2 | gate/e2e | §13.5 ("a gate you do not trust is not a gate") | A flaky test in the `light` Playwright project silently skips the entire `desktop` project, which is the whole of §13.5's `bun run test:e2e`. light-theme.spec.ts:143 "the stored theme is already in force on the first painted frame" asserts `frame.rendered === 0` — the #root child count at the first requestAnimationFrame — as its guard that React had not mounted yet. That is a race between the browser's first paint and React's mount, and after a reload the bundle is warm in the HTTP cache, so React sometimes wins: the assertion fails with "React had already rendered, so this proves nothing", Received: 1. Measured 2 failures in 8 consecutive runs (25%), always with the same Received: 1. playwright.config.ts makes `light` a `dependencies` of `desktop`, so on those runs the output is "1 failed / N did not run" and no scanner, camera, offline, import or share e2e test executes at all. The config comment anticipates the coupling but not that the coupled test is nondeterministic. Same class as F6 (a gate that goes red at random) but worse: this one does not just go red, it silently does not run. | for i in $(seq 1 8); do CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome npx playwright test --project=light --grep 'already in force'; done → 6 pass, 2 fail, both at light-theme.spec.ts:158 Expected 0 / Received 1. With --project=desktop the failing run reports "1 failed / 5 did not run". | FIX | open | — |
| R6-SA-2 | S2 | gate/tests-e2e | §13.5 ("`bun run test:e2e` (Playwright, Chromium; includes offline ..., the fake-camera scan flow, import, and share fallbacks)"), §13.6 criterion 3 | **One flaky light-theme assertion takes the entire desktop e2e project down with it, so the §13.5 e2e leg can report red having never run 43 of its 47 tests.** `tests/e2e/light-theme.spec.ts:158` asserts `expect(back.rendered).toBe(0)` — that `#root` still had zero children at the first `requestAnimationFrame` after a reload. That is a race against React's mount, not a property of the app, and on a cold run straight after `bun run build` it lost: `Expected: 0 / Received: 1`. `playwright.config.ts` gives the `desktop` project `dependencies: ["light"]`, so Playwright reported `1 failed · 43 did not run · 3 passed` and `bun run test:e2e` exited 1 — the fake-camera scan flows, the offline flows, import, share fallbacks and every storage spec simply did not execute. Three immediate re-runs of `--project=light` were 4/4 green, so the failure is intermittent and the skip is total. Same family as F6 ("a §13.5 gate that goes red at random ... is a gate you stop reading") and R2-G, with the extra amplification that the *scan* slice's own e2e evidence is what gets silently withheld. `playwright.config.ts:31-38` already names the coupling as a known cost; this is the first measured instance of it biting. | `CHROMIUM_PATH=/opt/pw-browsers/chromium-1194/chrome-linux/chrome bun run test:e2e` on a cold tree → `1) [light] › tests/e2e/light-theme.spec.ts:143:1 › the stored theme is already in force on the first painted frame — Expected: 0 Received: 1` at light-theme.spec.ts:158; summary `1 failed / 43 did not run / 3 passed`, exit 1. Then `CHROMIUM_PATH=… npx playwright test --project=light` ×3 → `4 passed` each time. Full log: /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/e2e.txt | FIX | open | — |
| SB-2 | S2 | bench | §13.4 ('ROI cropping'), §13.6 crit. 4 | THE BENCH DECODES A TIGHT CROP; THE APP DECODES A 1920x1080 FRAME, AND THE GAP IS UP TO 35 pp. `bench/run.ts` hands ZXing a ~1050 px symbol in a ~1100 px image. The app calls `decodeFromStream`, and `@zxing/browser` draws the whole `<video>` onto its capture canvas at videoWidth x videoHeight — 1920x1080 under §6.3's `ideal` constraints — so the symbol is a band across a mostly empty field. Measured on identical symbol pixels (the symbol is never resampled; `frame` is `crop` composited unscaled and centred): crop 571/840 = 68.0%, frame 535/840 = 63.7%. The loss is concentrated in severe: code_128 62.5% -> 27.5% (-35.0 pp), code_39_i 47.5% -> 30.0% (-17.5), code_39 50.0% -> 37.5% (-12.5), data_matrix 50.0% -> 37.5%, qr_code 55.0% -> 50.0%. Clean and moderate are essentially unchanged. Mean decode time goes 14.3 ms -> 40.0 ms (2.8x); at clean it is 5.8 ms -> 28 ms. Consequence: every §13.6 margin in the tracked report is measured on an image the app never sees, and it is optimistic in the direction that matters. Same class as B2 (the bench was not the app's decode path), one layer further out: B2 fixed the decoder, this is the frame. B2a is not this — B2a compares camera against canvas on frames that are already padded; nothing has ever compared padded against cropped. | `bun run bench/frame-probe.ts --count 40` (new file, /home/user/vinreader/bench/frame-probe.ts). Full table in its stdout; totals line reads `crop: 571/840 (68.0%) ... mean 14.3 ms` and `frame: 535/840 (63.7%) ... mean 40.0 ms`. | FIX | fixed | `e038c3b` |
| SB-4 | S2 | scanner/qr | §4.6, §13.6 crit. 4 | `qr_code` CLEAN 98.5% IS NOT SAMPLING — IT IS THREE SPECIFIC VINs THAT NEVER DECODE, AND ZXing IS THE CAUSE. The clean tier applies no randomness, so this cell reads 98.5% at every one of the five seeds swept; the three misses are the same three every time and are `no_decode`, not a §4.2 refusal. They are `JNDPASB01AM0V0ENV`, `4ZSFK20F3LP75VH94`, `TU2K0DHC2CMN1YF2M`. All three fail on the app's canvas path at bwip-js scale 15 / quiet 8 (the corpus geometry), at scale 10 / quiet 8, and at scale 15 / quiet 16 — so it is not module size or quiet zone. Two of the three (`JNDPASB01AM0V0ENV`, `TU2K0DHC2CMN1YF2M`) ALSO fail when re-encoded by the `qrcode` npm package at ECC-M, margin 4, scale 15 — a completely different QR encoder — so this is `@zxing/library`'s QR reader failing on a pristine, undegraded symbol, not a bwip-js artifact. Data Matrix of the same 200 VINs has 0 clean failures. Consequence: `qr_code clean 98.5% < 99.0%` is a permanent §13.6 criterion-4 miss with a named cause; TRY_HARDER is already on, so no §4.6 hint can move it, and any remedy is either a second decode attempt or an accepted limit in §13.7. NEEDS-ZACH because it cannot be closed inside §4.6 and it decides whether §13.6 crit. 4 is reachable at all. | `bun run bench --count 200` -> `qr_code clean: 98.5% < 99.0% (197/200 correct)`, identical at seeds 0x11111111 / 0x2bad5eed / 0x7f3ac91d / 0xdecafbad. Named directly by decoding `degrade(await renderBarcode(vin, "qr_code"), "clean", 1)` for the 200 corpus VINs on the canvas path: 3 failures, the three VINs above; `data_matrix` under the same loop: 0 failures. Cross-encoder: `QRCode.toBuffer(vin, { errorCorrectionLevel: "M", margin: 4, scale: 15 })` -> `JNDPASB01AM0V0ENV` and `TU2K0DHC2CMN1YF2M` still no-decode, `4ZSFK20F3LP75VH94` and `1HGCM82633A004352` decode. | NEEDS-ZACH | open | — |
| TA1 | S2 | storage/vin | §5.5, §4.5, §5.1 | The §5.5 WMI cache is write-only and the §4.5 seed is empty, so `structural.manufacturerFromWmi` is null for every VIN ever scanned and §4.5's "the seed grows with use" has no effect at all. `decodeQueue.applyDecodeResult` writes `db.wmi` (that half works and is asserted in the same file so this cannot be misread), and nothing in `src/` reads it. `buildStructural` resolves the manufacturer from the compiled-in `wmi-seed.json` alone, and that artifact holds 0 entries — `bun run seed:wmi` has never produced one. The table is never seeded from it on first run either, as §5.5 requires. Cost is exactly this app's case: a truck scanned with no signal shows no manufacturer, including one whose fleet-mate was decoded on the same phone an hour earlier; §6.2 drops the empty row, so it reads as "this vehicle has no manufacturer" rather than as anything being wrong — the R4-K shape. Regenerating the seed needs vPIC egress, which this sandbox does not have, so the seed half may be a human step. | `bunx vitest run src/lib/storage/wmiCache.test.ts` → 2 RED (named "FAILS: …"). `node -e 'console.log(Object.keys(require("./src/lib/vin/wmi-seed.json")).length)'` → 0. `grep -rn 'db\.wmi' src/` → one write at decodeQueue.ts:106, no read anywhere. | FIX | open | — |
| F10 | S3 | import/microcopy | §6.4 ("A payload that parses but carries no usable VIN: *That payload's VIN isn't 17 valid characters, so there is nothing to save.*"), P7 | The §6.4 line for this state can never render, and what renders instead is an internal schema message quoting a spec section. `ImportScreen.tsx:45` holds ERR_BAD_VIN verbatim, and both guards that use it — `readLink` at :189-191 and `readPaste` at :346-348 — test `isVinGrammarValid(payload.vin)` *after* `decodePayload`/`parseCarrier` have already validated the same field with the same regex (schema.ts:57 `z.string().regex(VIN_RE, …)`, VIN_RE at grammar.ts:8), so the guard is dead: a payload with a bad VIN always throws `PayloadError("schema")` first and codec.ts:107-111 renders its message. Rendered in the browser for `VINRELAY1:` + base64url of `{"v":1,"vin":"123"}`: title "This payload is not a VIN Relay record: vin not a VIN (§4.1: 17 characters, no I, O or Q)", body "Ask the sender to share it again, or paste the VIN below." (the §6.4 hint half is correct). Same class as R3-F4 — an untranslated internal string in front of a field user — on a screen R3-F4 does not name. | npx playwright test --config /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/pw.config.ts --project=phone import -g "pasted junk" → the "IMPORT payload no usable VIN" block; `grep -n ERR_BAD_VIN src/features/import/ImportScreen.tsx` → 45, 190, 347. | FIX | fixed | a55ab2b |
| F11 | S3 | scan/layout | §6.1 ("Portrait and landscape both work"; targets ≥48 px) | In landscape the scan screen overflows and the keyboard fallback is invisible. The preview keeps `aspect-[3/4]` and `max-h-[60vh]`, so at 844×390 it renders 414×234 with wide empty side margins while the column below it runs past the fold: "Type VIN instead" 48×416 with 0 visible px (hit test returns the bottom nav), and the §6.4 aiming line "Point at the barcode on the door-jamb sticker." clipped to 27 of 49.5 px. At 640×360: same button 0 px, status line 15 of 49.5 px. main scrollHeight 460 vs clientHeight 341 (844×390) and 442 vs 311 (640×360). Scanning itself still works, so this is the fallback and the status line rather than the camera. Related to F7 (same column, same cause) but reproduces in `streaming` with no error state at all. | npx playwright test --config /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/pw.config.ts --project=phone streaming — the 844×390 and 640×360 blocks. | FIX | open | — |
| F4 | S3 | ui/Banner | §6.1, F4 (existing row) | F4's predicted seventh call site exists today: `AccountScreen.tsx:417` renders `<Button disabled={locked} onClick={…}>` — no `variant`, so `variant="primary"` — inside a `Banner actions` row with no `className="h-14"`, so `[&>*]:min-h-[var(--tap)]` (Banner.tsx:36) wins and the §9-S4 "Add N local records" primary computes 48 px instead of the 56 the variant documents. I could not render it here (the build carries no Supabase configuration, so /#/account shows "Sign-in isn't set up in this build"), so this is the code path plus F4's already-measured mechanism, not a fresh measurement. A third workaround for the same broken contract has also appeared since F4 was written: `HistoryTable.tsx:30` pins `COPY_TARGET = { minHeight: "var(--tap-lg)" }` as an inline style with a comment explaining that it has to beat the button's own min-height. Recorded against F4 rather than as a new row. | grep -n "variant" -B4 src/features/account/AccountScreen.tsx \| sed -n '/415,425/p'; read AccountScreen.tsx:414-427 against Banner.tsx:36 and Button.tsx:57. Mechanism already measured in the ledger (F1-a, F4). | FIX | fixed | 2632c6b |
| F6 | S3 | sync/storage | §4.12, §13.5 | Still reproduces; not stale. The first full `bun run test` of this audit went red on scenarios.test.ts > "a delete followed by a re-scan" > "leaves the record alive on both sides" with deleted_at "2026-09-04T12:00:00.004Z" where null was expected — the same same-millisecond tie the row describes. A later run of the same suite was green, so the row's "intermittent" characterisation is intact. Reported only so the orchestrator does not mark it stale on a green run. | bun run test — 1 failed \| 1277 passed on the first run of this session, at src/lib/sync/scenarios.test.ts:235; green on the second. | FIX | open | — |
| F9 | S3 | history/wide | §6.6 ("History becomes a table (VIN · Year · Make · Model · Unit · Last scanned · Status · Copy) with the Sheet in a right-hand pane"), §6.5 ("On the wide layout every row has a copy button") | Two of §6.6's eight columns are never on screen at any wide width. The table's min-content width exceeds its scroll container at every width tested, including the default desktop one. At 1280×900 with ONE record and no long values: `div.overflow-x-auto` clientWidth 770, scrollWidth 950; the Copy column starts at x=880 and the container ends at 786, so each row's "Copy row" button measures 62.63×56 with 0 visible px and `elementFromPoint` at its centre returns the side pane's "Choose a row to see that vehicle here."; the Status chip is sliced mid-word ("Details"). One row carrying the longest §6.4 status chip ("Details failed — tap to retry", which the Chip renders `whitespace-nowrap`) takes the table to 1110 px. At 900×800 — the exact §6.6 threshold — the container is 472 px against a 1041 px table, so Status and Copy are both entirely outside it. Nothing indicates the horizontal scroll (overlay scrollbars); keyboard Tab does reach the button and scrolls it into view, which is why this degrades rather than blocks. | npx playwright test --config /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/pw.config.ts --project=wide table2 (minimal, 1280×900) and table900 (900/1000/1100/1280). Screenshot: /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/fa7/table1280.png | FIX | fixed | 84fcd5d |
| G3 | S3 | storage/scan-write | §5.7, §4.12, N7, P1 | The §5.7 outbox grows without bound from the scan path, and nothing prunes it on a device that is never signed in. Every upsertVehicle appends two rows: a scan_event (keyed by the event id, so re-appending is idempotent) and a vehicle_meta keyed by a fresh newId(), so it is not. vehicle_meta carries the whole record including p_decode — every vPIC field — and §4.12 resolves it last-writer-wins per VIN, so only the newest row for a VIN can ever change anything. push.ts runs only from the sync engine and §9-S4's sign-out is the only other caller of clearOutbox, so signed out the outbox is a write-only store fed by every scan. Measured: 51 scans of ONE decoded VIN leave 102 outbox rows and ~305 KB, 5,969 bytes per re-scan (a 5,003-byte decode block plus a 388-byte scan_event), and among the 51 vehicle_meta rows there are 2 distinct payloads — 49 byte-identical duplicates. A crew rescanning 200 already-decoded trucks a day queues ~1.2 MB/day it will never read, invisibly (§6.4 gives a signed-out device no sync chip), until QuotaExceededError lands on the scan path and §6.4's "Couldn't save this VIN" is the first the user hears of it. Second-order, on the day they do sign in: dueRows() reads the WHOLE table into a JS array before filtering and slicing to 50 (outbox.ts:150-160), and a push cycle does that up to PUSH_MAX_DRAINS = 10 times. | bun run test -- src/lib/storage/outboxGrowth.adversary.test.ts — 2 RED: {metaRows: 51, distinctPayloads: 2} where at most 2 rows can matter; 5,969 bytes per re-scan against a 5,003-byte decode block. src/lib/storage/outbox.ts:83-92 (fresh newId per meta row) against outbox.ts:57-70 (scan_event reuses the event id and says why). | FIX | **fixed** | 1ec0fb4 — `vehicleMetaRowId(vin)` keys the row by its VIN, so a re-queue replaces; `removeOutboxRows` takes rows and deletes only an unchanged payload, so a write landing during an in-flight push is not dropped. 2 RED green. Queues written by an earlier build keep their old rows: this stops the growth, it does not reclaim |
| G4 | S3 | storage/model-year | §4.4 step 0, N2 | A device clock that lost years turns §4.4's cap into a wrong year shown as a fact. modelYearFromVin is pure and takes the current year as an argument exactly as §4.4 requires, but every caller passes new Date().getFullYear() with no floor under it (upsert.ts:81; normalize.ts via SheetScreen and HistoryScreen). §4.4 step 0 drops "any candidate greater than the current year + 1", so a clock reading earlier than the truck makes the truck look like the future and the rule written to refuse a future year refuses the real one. 1FUJGLDR0PLBT1234 (check-digit valid, position 7 a letter, position 10 P = 1993/2023) resolves to 2023 on a correct clock; on a phone whose clock has fallen back to 2016 — an Android that lost its RTC, a handset left flat in the cold, a factory reset with no signal to fetch time from — it resolves to 1993, one year, no "1993 or 2023", no ambiguity note. That value is written to the §5.1 record and queued in §5.7's vehicle_meta. §4.4 says nothing about what the caller may pass, so a floor is a caller-side change and not a constant change; §4.4 already tolerates the case (with no survivors it returns {candidates: [], resolved: null} and §6.2 drops the row). Related documentation-only mismatch: §4.4 asserts "the early candidate is never dropped … so at least one always survives", which is false for any currentYear below 2008 — the code handles it, the sentence does not. | bun run test -- src/lib/storage/modelYearClock.adversary.test.ts — 2 RED. vi.useFakeTimers({toFake:["Date"]}) at 2016-05-01, upsertVehicle(1FUJGLDR0PLBT1234) → structural.modelYear.resolved === 1993 and the same 1993 in the queued vehicle_meta p_structural. modelYearFromVin(VIN, 2026) === {candidates:[1993,2023], resolved:2023}. | FIX | **fixed** | eea7081 — `currentYear()` beside `nowIso()` floors the clock at `__BUILD_YEAR__` (the build's commit year, injected by the three configs from scripts/build-year.ts); all five callers take it from there. No §4.4 constant moved. 2 RED green, plus four tests on `currentYear` itself in db.test.ts. The §4.4 prose mismatch this row notes ("at least one always survives" is false below currentYear 2008) is untouched — spec text, NEEDS-ZACH |
| R6-SA-1 | S3 | scan/decoder | §4.6 ("`TRY_HARDER = true`"), §9-S1 ("confirmed read within ~2 s ... If real-label decode rate is poor"), §11 ("1D barcodes on curved, scuffed, glared labels are the hard part of this whole product") | **§4.6's `TRY_HARDER` 90°-rotated retry is dead in the running app, and it throws once per miss frame.** `@zxing/browser` 0.2.1's `HTMLCanvasElementLuminanceSource` never assigns `tempCanvasElement` in its constructor, so `getTempCanvasElement()`'s `if (null === this.tempCanvasElement)` test is false against `undefined`, returns `undefined`, and `rotate()` throws `Error: Could not create a Canvas element.` (node_modules/@zxing/browser/esm/common/HTMLCanvasElementLuminanceSource.js:111-125). `isRotateSupported()` still answers `true` (:98), so `OneDReader.decode` takes the branch every time the horizontal pass misses and `TRY_HARDER` is set (node_modules/@zxing/library/esm/core/oned/OneDReader.js:44-47). The throw is not a `ReaderException`, so it escapes into `MultiFormatReader.decodeInternal`'s per-reader catch, which `console.warn`s and moves to the next reader (MultiFormatReader.js:201-207). Net effect: the app sets the §4.6 hint exactly as specified and gets only half of it — a 1D symbol at 90°/270° in the frame is never recovered, and every frame whose horizontal 1D pass misses logs a warning. The app code is right; the hint is inert. Filed here rather than as a §4 change because the constant's *value* is not in question — §4.6 says `TRY_HARDER = true` and `buildScanHints()` sets `true`. §9-S1 already permits an in-slice decode optimisation ("crop the video frame to the guide box on a canvas before decoding ... optimisation is allowed inside this slice; report it"), so rotating the frame ourselves, or supplying a luminance source that can rotate, is in scope; so is recording it as a locked-stack limitation. How much it costs depends on how many labels in this fleet print the barcode rotated, which is §13.7's to answer. | Measured through the app's own reader, built from `buildScanHints()`, in Chromium (/opt/pw-browsers/chromium-1194/chrome-linux/chrome), Code 39 and Code 128 of `1HGCM82633A004352` rendered with bwip-js and drawn to a canvas at four rotations: ``` rotateProbe: {"isRotateSupported":true,"rotate":"THREW: Could not create a Canvas element."} code_39  rot   0 {"ok":true,"text":"1HGCM82633A004352"} code_39  rot  90 {"ok":false,"error":"NotFoundException2"} code_39  rot 180 {"ok":true,"text":"1HGCM82633A004352"} code_39  rot 270 {"ok":false,"error":"NotFoundException2"} code_128 rot  90 {"ok":false,"error":"NotFoundException2"} ``` Also observed live: browsing https://localhost:4321/#/scan under Chromium's fake camera logs `MultiFormatReader: non-ReaderException from reader: Error: Could not create a Canvas element. at HTMLCanvasElementLuminanceSource.rotate at ...rotateCounterClockwise`. Probe script: /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/rot.mjs with /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/entry.ts | FIX | open | — |
| R6-SA-3 | S3 | ui/targets | §6.1 ("Targets: ≥ 48 px everything; ≥ 56 px for Scan, Use as-is, Share, Copy, Sign in") | **Both controls the app labels "Scan" measure 48 px, where §6.1 names Scan in the ≥ 56 px list.** The bottom-nav `Scan` tab carries `min-h-[var(--tap)]` (src/app/nav.tsx:24) and the button that returns from typed entry to the camera, `Scan with the camera`, is `variant="secondary"`, which pins `min-h` to `var(--tap)` = 48px (src/features/scan/ScanScreen.tsx:220 with src/ui/Button.tsx VARIANTS). Measured in Chromium at 390×844: nav `Scan` 48.00 × 130, `Scan with the camera` 48.00 × 358. This is exactly the class A14 was filed and fixed under for Copy ("§6.1 names Copy in the ≥ 56 px list"), and the rule is understood elsewhere in the same screen — `Rescan` and `Scan again` both carry `className="h-14"` and measure 56, and History's `Go to Scan` measures 56. Round 3's "Targets: nothing under 48 px in any probed state" checked the floor, not the named-control ceiling, which is why this survived. If the orchestrator reads §6.1's "Scan" as the primary scan action only and not the nav tab, `Scan with the camera` still fails on its own. | Chromium at 390×844 against `vite preview` of the current build, bounding boxes of every button/link on `#/scan`: ``` ### scan (camera mode)   48 x 130  A  "Scan"   (bottom nav) ### scan (manual mode)   48 x 358  BUTTON "Scan with the camera" 56 x 358  BUTTON "Save VIN" ### history empty        56 x 316  BUTTON "Go to Scan" ``` Script: /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/measure.mjs | FIX | fixed | abaa5b3 |
| SB-3 | S3 | scan/useScanner | §13.4 ('the bench tunes ROI cropping'), §6.1 | AN ROI CROP IS WORTH +12.5 pp ON THE WORST 1D SEVERE CELL AND -27% DECODE TIME — BUT NOT THE GUIDE BOX AS DRAWN. §6.1 draws a guide box over the preview (`CameraView.tsx:92`, `h-[22%] w-[90%]`) telling the user where to put the label, and nothing downstream uses it: ZXing is offered every pixel. Two ROI bands measured on the same frames as SB-2. (a) Cropping to the drawn box, 90% x 22% = 1728x238 px, is CATASTROPHIC for 2D: data_matrix clean 100% -> 0%, qr_code clean 95% -> 0%, overall 392/840 = 46.7%, because a label-realistic Data Matrix or QR is ~480-500 px tall and the band is 238. Any fixer reaching for 'crop to the guide box' must not use the box as drawn. (b) A taller centred band, 90% x 40% = 1728x432 px, gives 547/840 = 65.1% against the full frame's 63.7% (+1.4 pp overall) with per-cell gains where they are needed: code_128 severe 27.5% -> 40.0% (+12.5), code_39_i severe 30.0% -> 40.0% (+10.0), code_39 severe 37.5% -> 40.0%, and 2D fully restored (data_matrix clean/moderate back to 100%, qr_code clean back to 95%). Mean decode time 40.0 ms -> 29.0 ms. It does not reach the tight crop's 68.0%, so ROI recovers about a third of what SB-2 measures as lost. Separately: a guide box the decoder ignores is telling the field user something that is not true. | `bun run bench/frame-probe.ts --count 40` — the `roi` and `roi_tall` columns; the band fractions are `GUIDE_WIDTH_FRACTION`/`GUIDE_HEIGHT_FRACTION`/`TALL_GUIDE_HEIGHT_FRACTION` in /home/user/vinreader/bench/frame-probe.ts, taken from CameraView.tsx:92. | FIX | open | — |
| SB-5 | S3 | bench | §13.4 ('Report: decode rate ..., mean time-to-confirm, and above all false accepts') | MEAN TIME-TO-CONFIRM IS ONE OF THE THREE THINGS §13.4 ASKS THE REPORT FOR AND IT HAS NEVER BEEN MEASURED. `bench/report.md`'s timing section says so in prose ('§13.4's mean time-to-confirm is not here'), the ledger's round-4 narrative says so at HARDENING_S1.md:499 ('Time-to-confirm is still not measured'), and there is no ledger ROW for it — a stated omission with no id, no severity and no owner. I have now measured it. `bench/confirm-probe.ts` is §13.4 run (b): the built app served from loopback (a secure context, so getUserMedia and crypto.randomUUID behave as on the phone), Chromium's own `--use-file-for-fake-video-capture` at 1920x1080, 12 DISTINCT degraded poses at 10 fps drawn from the same seed arithmetic run.ts uses (one image repeated would confirm on the second frame by construction and measure nothing), a fresh browser context per repeat so §6.3's 10 s cooldown is not what gets timed, clock running from the first frame the <video> element can supply to the hash change only the `confirmed` transition performs. 28 runs, 28/28 confirmed inside 25 s: code_39_i clean 268 ms, moderate 450 ms, severe 765 ms; code_128 clean 285 ms, moderate 279 ms, severe 1534 ms; overall mean 530 ms. The number worth the fixer's attention is the last one: code_128 severe at 1534 ms mean is ABOVE §6.3's 1.5 s agreement window, so on a severe label the standing candidate typically lapses at least once and confirmation restarts. The probe is not yet in `bun run bench` — it is a separate command, and one Chromium launch flaked at the 180 s Playwright default during the run, so it needs a retry policy before it belongs in the §13.5 gate. | `bun run bench/confirm-probe.ts --repeats 5` (new file, /home/user/vinreader/bench/confirm-probe.ts; needs `dist/`). Absence of the number: `grep -n 'time-to-confirm' bench/report.md hardening/HARDENING_S1.md` -> one prose line each, no ledger row. | FIX | open | — |
| SB-6 | S3 | bench | §13.4, §13.5, §13.6 crit. 4 | R2-E's GUARD IS KEYED ON THE FLAGS THAT BROKE IT, NOT ON WHETHER THE RUN IS CANONICAL — AND A 2-ATTEMPT RUN NOW WRITES 'PASS' INTO THE TRACKED §13.6 EVIDENCE. `run.ts:1419` reads `const nonCanonical = options.quick \|\| options.severeExtras !== null \|\| app !== APP_PATH;`. `--seed`, `--count`, `--tiers` and `--symbologies` are not in it, so any diagnostic run that keeps the app's decode path first overwrites `bench/report.md` and the `--json` target. Demonstrated, not reasoned: `bun run bench/run.ts --count 2 --tiers clean --symbologies code_39 --paths canvas` moved bench/report.md from md5 d863d5f3f1f1ec6a9fb58a31f72aa66e to 343e400f56e7dafe4fba0209973fefea, and the replacement's headline is '**PASS** — every §13.6 threshold met.' over `Attempts \| 2`. This is the fourth occurrence of the class: round-1 review N-01, R2-E, the Z1 commit message, and now this. I hit it myself four times running the seed sweep and had to restore from a backup each time. Smallest fix, in run.ts and therefore in bench scope: extend the predicate to `\|\| options.seed !== DEFAULT_SEED \|\| options.count !== DEFAULT_COUNT \|\| options.tiers.length !== TIERS.length \|\| options.symbologies.length !== BENCH_SYMBOLOGIES.length`. Reported rather than applied because changing the canonicality rule mid-audit would move the artifact the orchestrator is about to read. | `md5sum bench/report.md; bun run bench/run.ts --count 2 --tiers clean --symbologies code_39 --paths canvas; md5sum bench/report.md; head -3 bench/report.md` | FIX | fixed | `33ebfd0` |
| SB-7 | S3 | bench | §13.4, §13.6 crit. 4 | A SINGLE-SEED CELL CARRIES UP TO 11.5 pp OF SEED NOISE, WHICH NOTHING IN THE REPORT SAYS. Five full canvas runs at five seeds, 200 attempts per cell. Spread (max minus min) per cell: code_128 severe 11.5 pp (54.0-65.5), code_128 moderate 9.0, code_39_i severe 8.5, data_matrix severe 8.0, qr_code severe 7.5, code_39 moderate 6.0, code_39 severe 4.5, code_39_check severe 4.0. Every clean cell has spread 0.0, because the clean tier applies no randomness — which is itself worth knowing, since it means a clean-tier miss is always structural (see SB-4). Means over the five seeds: code_39 100.0/80.1/48.8, code_39_i 100.0/77.5/47.4, code_39_check 24.0/18.5/12.1, code_128 100.0/80.2/60.7, code_128_fnc1 100.0/76.2/0.9, data_matrix 100.0/96.3/59.1, qr_code 98.5/97.8/44.8. Consequence for the loop: the tracked report presents one seed's cell as the number, and a fixer claiming a hint change moved a moderate or severe cell by less than ~5 pp on one seed has measured noise. None of the §13.6 gaps closes under this band — code_128 moderate is still 9.8 pp short at its best seed — so no verdict changes; the reporting does. Cheapest fix: print the tier-level spread, or add a `--seeds` mode that reports a band. | `bun run bench/run.ts --paths canvas --seed <s> --json <out>` for s in 0x11111111, 0x2bad5eed, 0x7f3ac91d, 0xdecafbad, against the tracked 0x5eed1a7c run; compare `cells[].decodeRate` where `path == "canvas"`. (Note SB-6: back the tracked report up first.) | FIX | fixed | `80114a1` |
| SB-8 | S3 | bench | §13.7 R5 question (b), §4.6 (`stripAimIdentifier`, ASSUME_GS1) | THE LEADING-FNC1 COST IS NOW MEASURED, AND §4.6's AIM STRIP HAD ZERO BENCH COVERAGE UNTIL IT WAS. `bench/report.md`'s header has printed 'Reads carrying the §4.6 AIM identifier: 0' since `stripAimIdentifier` shipped, because no row in BENCH_SYMBOLOGIES opens with FNC1 — the most recently added §4.6 guard, on the app's scan path and on the bench's, had never had a byte through it from a real decode in the gate. `bench/fnc1-probe.ts` renders the two shapes §13.7 (b) asks about, as probe-only rows kept OUT of BENCH_SYMBOLOGIES so the tracked §13.6 threshold list does not move without anyone deciding. 60 VINs x 3 tiers, canvas path, each decode scored twice off the same bytes — once as the app sees them, once with `]C1` put back. `code_128_fnc1_lead` (FNC1 + VIN): shipped 100.0% clean / 81.7% moderate / 68.3% severe; unstripped 0.0% at EVERY tier. `code_128_fnc1_lead2` (FNC1 + VIN + FNC1 + 1P part number): shipped 100.0% / 85.0% / 0.0%; unstripped 0.0% at every tier. `]C1` was present on 150 of 180 and 112 of 180 reads respectively — the strip fires on every decode of those rows. Zero false accepts either way; the strip introduces none. So §13.7 (b)'s cost is total: without the strip a leading-FNC1 Code 128 VIN label is unreadable at any tier, and with it it reads as well as a plain Code 128 label. A second result falls out: `code_128_fnc1_lead` severe reads 68.3% where `code_128_fnc1` severe reads 0.9%, which confirms the corpus's own geometry explanation for that near-zero cell (one field is 200 modules at ~5 px, two fields are 310 modules at ~3 px, halved again at severe) rather than anything to do with FNC1 handling. How many real labels carry the shape stays §7 item 4. | `bun run bench/fnc1-probe.ts --count 60` (new file, /home/user/vinreader/bench/fnc1-probe.ts; the two probe rows are `PROBE_SYMBOLOGIES` in bench/corpus.ts). | FIX | open | — |
| TA2 | S3 | tests | §5.4, §13.5 | A guard that cannot fail, same class as R4-H' and F1-a. `decodeQueue.env.test.ts`'s "polls only while the document is visible" runs both branches of `isVisible()` — so §13.5's branch coverage is satisfied — and then asserts `fetchImpl.mock.calls.length >= whileHidden` against a database with nothing pending, which is true of every possible implementation including one with no visibility check at all. That is why `bun run mutate` reported ten survivors on a nine-line function. §5.4's "every 60 s while the app is visible and online" was therefore unmeasured in both clauses. | src/lib/storage/decodeQueue.env.test.ts:56-60. Hand-applying `isVisible() → true`, `→ false`, body `{}`, `\|\|→&&` and both string-literal mutants leaves that file green in all six cases (verified in a scratch copy). Closed by src/lib/storage/decodeQueue.visibility.test.ts, which kills all ten plus the onTick `trigger()` removal and the uncleared interval; the vacuous assertion should be deleted so it cannot be read as cover. | FIX | open | — |
| TA6 | S3 | gate/config | §13.5, A23-a, A23-c | `stryker.config.json`'s `ignorePatterns` still excludes `src/lib/storage/db.test.ts`, and its own comment says "Delete this entry the moment db.test.ts stops depending on the pool". A23-a fixed exactly that (the zone now reaches `nowIso` through a stubbed Date rather than `process.env.TZ`), so the exclusion is stale and now costs score: §5.1's offset arithmetic is still measured with its own test switched off. Measured, three mutants in `nowIso`: with db.test.ts they die, with the mutation run's file set they survive with no non-adversary test failing at all. | `bunx vitest run src/lib/storage/db.test.ts --pool=threads` → 7 passed (the row's repro said 5 failed). Then, in a scratch copy: `abs % 60` → `abs * 60` → db.test.ts 6 failed, rest of the suite no new failure; `pad(date.getMonth() + 1)` → `- 1` → same; `offsetMinutes < 0` → `<= 0` → db.test.ts 1 failed, rest none. | FIX | open | — |
| F12 | S4 | app/microcopy | §6.4 tone ("terse, plain"), P7, §0 rule 4 (the boundary's supplied strings) | The F1 error-boundary screen prints the engine string twice on one line. `describe()` (ErrorBoundary.tsx:78-82) is commented "The thrown value as one line the user can read out over a phone (P7)" and returns `${error.name}: ${error.message}`, but Dexie's rejected live query carries the name inside the message and an embedded newline, so the rendered `<p>` textContent is "UnknownError: Connection to Indexed Database server lost\n UnknownError: Connection to Indexed Database server lost" at 14 px. Same class as R3-F4 (which names useVinCommit.ts:83 and renders "storage full QuotaExceededError: storage full" on the scan path — still reproducing, measured this round) at a second site R3-F4 does not name, so a fix scoped to the write path will leave this one. | npx playwright test --config /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/pw.config.ts --project=phone boundary2 — seeds a row, then breaks `IDBIndex.prototype.getAll/openCursor` and reloads; prints every `<p>`. | FIX | fixed | edb80f5, 0dda470 |
| F13 | S4 | ui/a11y | §6.6 (keyboard/assistive reachability), §13.2 (axe-core on every screen) | Three states render no level-1 heading, and axe-core flags each with `page-has-heading-one` (moderate, 1 node, target `html`). (1) the Sheet's no-record branch — `NoRecord` at SheetScreen.tsx:199-211 renders a bare `VinDisplay` span plus a `<p>`; (2) the same screen's deleted-tombstone state ("Vehicle deleted"); (3) the F1 boundary screen ("Storage isn't available"). Every other screen and state I probed has one. Round 3's "what held" says the only moderate anywhere was `landmark-unique` in the R3-F2 state, so this is new ground rather than a known residue. Zero critical and zero serious violations across every state and viewport measured this round. | npx playwright test --config /tmp/claude-0/-home-user-vinreader/c79fd7d1-8827-5e7c-aa59-8ca5f7602e93/scratchpad/pw.config.ts --project=phone states2 ("SHEET unsupported" = /#/v/<unknown vin>), delete ("SHEET deleted"), boundary2 — each prints AXE [{"id":"page-has-heading-one","impact":"moderate","n":1}]. | FIX | fixed | df889bc |
| R6-SA-4 | S4 | bench/evidence | §13.4 ("Report: decode rate per symbology × tier ..."), §13.2 (findings carry numbers), §13.5 | **The bench report states a decode mechanism that cannot happen, and the ledger repeats it.** `bench/run.ts:900-901` prints, into the tracked `bench/report.md`, that the difference between the two instruments is "`isRotateSupported()` — true only on the canvas source, so `OneDReader` gets a 90°-rotated retry under `TRY_HARDER`, which cannot help a symbol that is already horizontal"; the ledger's B2 row (line 22) and the round-5 B2 write-up (line 475) say the same. The retry never runs — it throws (R6-SA-1), so one of the two named mechanisms behind the all-zero canvas/rgb delta is fictitious. `bench/browser-entry.ts:14-15` states the same thing about the rgb path, correctly, and then draws the wrong conclusion about the canvas path. The delta itself is unaffected — zero either way — so this is the *explanation* being wrong, not the number. Same class as R4-B and R4-H': a recorded assurance whose mechanism was never checked. | `grep -n "isRotateSupported" bench/run.ts` → :900-901; the same sentence is in the tracked `bench/report.md`. Contradicted by the rotateProbe result in R6-SA-1. | FIX | open | — |
| R6-SA-5 | S4 | constants/microcopy | §7 item 5 ("no constant is defined in more than one place in the code"), §6.4 ("Copied ✓"; "Copying isn't available here…"), §6.5 ("confirmed by a *Copied ✓* toast (1.5 s)") | **Three §6.4/§6.5 constants are literal duplicates across the two copy implementations.** `const COPIED = "Copied ✓"` at src/features/sheet/Actions.tsx:16 and src/features/history/copy.tsx:6; `const COPIED_MS = 1500` at Actions.tsx:19 and copy.tsx:9 (§6.5's 1.5 s toast, with the same doc comment in both); and `MANUAL_COPY_PROMPT`, the whole §6.4 no-Clipboard-API sentence, at Actions.tsx:32-34 and copy.tsx:18-20. This is R3-J's class one screen over — R3-J names only the `ScanScreen.tsx` / `ManualEntry.tsx` pair — and the drift hazard is recorded history in this ledger twice already (round-1 review N-03, and R3-I, where the same sentence lost its full stop in one of two places). Reported separately from R3-J because it is a different file pair and different strings, so closing R3-J does not close it. | `grep -rn "COPIED_MS\s*=\\|MANUAL_COPY_PROMPT\s*=\\|const COPIED =" src/` → two hits each, in `src/features/sheet/Actions.tsx` and `src/features/history/copy.tsx`. | FIX | open | — |
| SB-9 | S4 | gate/mutation | §13.5, ledger A23-a / A23-c | THE STRYKER EXCLUSION FOR `db.test.ts` IS STALE, AND ITS OWN COMMENT SAYS WHEN TO DELETE IT. `stryker.config.json:17` carries an `_ignoreComment` explaining that `src/lib/storage/db.test.ts` is excluded because 5 of its 7 tests set `process.env.TZ` at runtime, which only moves the clock under vitest's `forks` pool, and it ends 'Delete this entry the moment db.test.ts stops depending on the pool.' It has stopped: `npx vitest run src/lib/storage/db.test.ts --pool=threads` now reports 7 passed, 0 failed. The file is still listed at `stryker.config.json:27`, so `src/lib/storage/db.ts`'s `nowIso` offset arithmetic is still being mutation-scored with its own test excluded, and the config's stated reason no longer holds. Config is not mine to edit; this is a finding. | `npx vitest run src/lib/storage/db.test.ts --pool=threads` -> `Test Files 1 passed (1) / Tests 7 passed (7)`; `grep -n 'db.test.ts' stryker.config.json` -> still excluded at :27 with the stale rationale at :17. | FIX | open | — |
| TA3 | S4 | storage | §5.4, §13.5 | `startDecodeQueue`'s `stopped` flag is unreachable state. The teardown removes the "online" listener and clears the interval, so nothing can call `trigger()` after `stop()` and the `if (stopped) return;` body can never execute. Consequences: decodeQueue.ts:258 is the only uncovered line left in `src/lib/storage`, and the `stopped = true → false` mutant is unkillable by any test — I confirmed it survives all 34 tests now covering the file. Same class as R3-T1 (`client.ts:134` unreachable): a finding about the code, not the tests. Either delete the flag or state at the function why it is kept. | `bun run test:coverage` → `decodeQueue.ts … Uncovered Line #s 258`. Applying `stopped = true` → `stopped = false` in a scratch copy: `bunx vitest run src/lib/storage/decodeQueue*.test.ts` stays fully green. | FIX | open | — |
| TA4 | S4 | vpic/gate | §4.7, §13.5 | Amends M7: one of the four survivors it lists cannot be killed by any test, because it is an equivalent mutant. `client.ts:109` reads `!Array.isArray(results) \|\| results.length === 0 \|\| !isRecord(results[0])`; for an empty array `results[0]` is `undefined`, so `!isRecord(undefined)` is already true and the middle clause changes nothing. §13.2's empty-`Results` case IS distinguished — by the third clause. M7's other three (the `finally { clearTimeout }` block, its call, and the `response.json()` catch body) are killed by the new client.lifecycle.test.ts; M7's `"Unknown error"` NoCoverage is R3-T1, already open. | In a scratch copy, `results.length === 0` → `false`: `bunx vitest run src/lib/vpic/` → 32 passed, unchanged from baseline. Contrast the `finally` block emptied → 4 failed, and the `catch` body removed → 1 failed. | WONTFIX | open | — |
| TA5 | S4 | tests | §13.2 ("property-based tests"), §13.5 | Seven `fc.assert` calls in `src/lib` ran unseeded, so the §13.5 gate drew a different sample on every run: extractVin.test.ts (1), modelYear.test.ts (1), checkDigit.test.ts (1) and codec.test.ts (4) — the round-trip, the base64url shape, the carrier round-trip and the D14 guard law among them. A property that fails once in a hundred rounds is F6's problem again: a gate that goes red at random is a gate people stop reading, and an unreproducible counterexample is not a repro. Fixed in the test layer this round: each now carries an explicit seed and numRuns; nothing else about the assertions changed and all seven still pass. | Before: `grep -c 'seed:'` → 0 in all four files against 7 `fc.assert` calls. After: seeds 0x4a2_0001, 0x4a3_0001, 0x4a4_0001 and 0x4a9_0001-0004; `bunx vitest run src/lib/vin/ src/lib/payload/codec.test.ts` green. | FIX | open | — |

| SB-10 | S3 | bench | §13.4, §13.6 crit. 4 | **The ROI section tells the next fixer what ROI buys and not what it risks.** `bench/report.md` records that a taller 90%×40% band buys back `code_128` severe 27.5% → 40.0%, and does not say the thing that makes that number dangerous: ROI raises the decode rate by making marginal frames decodable again, and a marginal Code 128 frame is exactly where both known checksum collisions live. The zero-false-accept headline holds *because* those frames currently read as nothing. Anyone who implements ROI must re-run the five-seed sweep before believing the headline still applies. | `bench/report.md` ROI section vs the false-accept headline | FIX | open | — |
| SB-11 | S3 | bench | §13.6 crit. 4, §13.5 | **The 21,000-attempt sweep result is hardcoded in the report generator and will be printed by every future zero-false-accept run.** `run.ts:1444-1456` emits the five-seed sweep paragraph as a literal. It is true today — the reviewer verified all 21,000 attempts and both standalone collisions — but it is a claim about a measurement that is no longer being taken, printed as though it were. The same class as the guards this ledger already records at R4-H′, F1-a and TA2: a statement that cannot become false because nothing recomputes it. | read `run.ts:1444-1456`; change any decode parameter and observe the paragraph print unchanged | FIX | open | — |
| FB-1 | S4 | history/wide | §6.6 ("History becomes a table (VIN · Year · Make · Model · Unit · Last scanned · Status · Copy) with the Sheet in a right-hand pane") | **What F9 left: below roughly 1200 px the table still scrolls sideways, and nothing indicates that it does.** F9 took the table's min-content width from 950/1110 px down to 725 by removing three `whitespace-nowrap` constraints, which puts all eight columns inside the 770 px container at 1280×900. The residual is arithmetic: at §6.6's own 900 px threshold the pane (`w-[min(42vw,460px)]`) leaves the table 472 px, and eight columns headed by a 17-character grouped VIN do not fit in 472 px at any type size this app is allowed to use (§6.1 letter-spaces and monospaces it). Measured after 84fcd5d with two seeded rows: 900 → client 472 / scroll 725, 1000 → 530 / 725, 1100 → 590 / 725, 1280 → 770 / 770. Overlay scrollbars mean nothing shows that the columns continue; Tab still reaches the Copy button and scrolls it into view, which is why this degrades rather than blocks. Closing it needs a decision this loop may not take on its own — a narrower pane, a column that drops below some width, or a stated scroll affordance — so it is recorded rather than guessed at. | `npx playwright test --config <scratch>/fixb/pw.config.ts --project=wide cols` (seeds one ambiguous-year row and one `Details failed — tap to retry` row, prints client/scroll per width); tests/e2e/history-table-columns.spec.ts pins the 1280 case | NEEDS-ZACH | open | — |
| FB-2 | S4 | ui/targets | §6.1, §7 item 5, F4 | **Two §6.1 targets are still pinned with a retyped 56.** `ScanScreen`'s and `ManualEntry`'s "Use as-is" carry `className="h-14"`, which is 56 px written as a spacing step rather than as `--tap-lg`. They are not F4's redundant pins — §6.1 names Use as-is in the ≥ 56 px list and the variant is `secondary`, so they say something the variant does not — but `Button.TAP_LG_TARGET` (abaa5b3) is now the way to say it without retyping the number, and `HistoryTable`'s `COPY_TARGET` inline `minHeight` is a third spelling of the same intent whose comment ("so it beats the button's own min-height") describes a `Button` those cells do not use. One idiom, three spellings, one of them documented wrong. | `grep -rn "h-14\|COPY_TARGET" src/` against `src/ui/Button.tsx`'s `TAP_LG_TARGET`; sizes themselves are green — tests/e2e/banner-targets.spec.ts and tests/e2e/scan-targets.spec.ts measure them | FIX | open | — |
| FB-3 | S4 | ui/a11y | §6.6, §13.2 | **§6.6's wide layout renders two `<h1>`s**, and always has: History's own heading, plus the Sheet's VIN heading inside the right-hand pane (`SheetScreen` renders `<h1><VinDisplay/></h1>` whether it is a route or a pane). axe is silent — `page-has-heading-one` asks for at least one — and F13's fix did not add this one, but a page with two level-1 headings describes itself twice to a screen reader. Noted while fixing F13 so the next round does not read it as new. | 1280×900, `/#/history` with one seeded row: `document.querySelectorAll("h1")` is 1 before a row is chosen and 2 after — `["History", "1HG CM826 3 3 A 004352"]` | FIX | open | — |


### Stale rows, proved non-reproducing

Several rows said `open` while the commit that fixed them had already landed. A ledger that
misreports its own state makes §13.6 criterion 1 unmeasurable, so these are closed on evidence.

| id | evidence | found by |
|---|---|---|
| A23 | `bun run mutate` exists and has been run. `package.json:25` carries `"mutate": "stryker run"`; `@stryker-mutator/core` and `@stryker-mutator/vitest-runner` 10.0.0 are in `devDependencies` and present under `node_modules/@stryker-mutator/` (api, core, instrumenter, util, vitest-runner); `stryker.config.json` is at the repo root. The same ledger's Round 5 section carries a second row with the same id `A23`, marked `fixed` at `900e541` (`git log --oneline -1 900e541` → "A23: `bun run mutate` exists, five rounds late"), and reports a completed run — 2,879 mutants, 36 files, 20 min 58 s, 85.01% on `src/lib/*`. The round-1 row is a duplicate id left `open`; its repro column ("`bun run mutate` → Script not found", "`ls node_modules \| grep -i stryker` → empty") no longer reproduces. | spec |
| B2 | The work is committed. `git log --oneline -1 370a21f` → "B2: the bench decodes through the app's reader — and the delta is zero". `bench/browser-entry.ts` and `bench/browser-decode.ts` are tracked and `git status --porcelain` is empty. Status `fixed` is right; only the commit column is stale. Flagging it under this ledger's own rule that "a ledger that misreports is the same defect as a guard that cannot fail". (Same class, one row down: R4-A''s commit column reads `9f5c357 c` — `9f5c357` resolves, the trailing ` c` is a typo.) | spec |
| R3-I | Fixed at 4e448f1 and rendering correctly. `src/features/sheet/StructuralBlock.tsx:20` now hoists `const NO_CHECK_DIGIT = "This number doesn't use a check digit."` with the full stop, and the running app renders it: at 390×844 on /#/v/WVWZZZ1JZ1W123456 the check-digit row's textContent is "This number doesn't use a check digit." (code points end 64 69 67 69 74 2e), identical to ManualEntry's chip. Probe: scratchpad/fa7/states2.spec.ts and sweep.spec.ts ("SHEET-AMBIG" / "SHEET unsupported" blocks). | field |
| R3-F7 | Fixed at 4e448f1. `StructuralBlock.tsx:32` defines `YEAR_PENDING = "— will confirm when details load"` and the caller emits the joining space, so the rendered `<dd>` for VIN 1HGCM8263TA004352 reads "1996 or 2026\n— will confirm when details load" — the em dash is present in textContent, which is exactly what R3-F7 measured as missing ("1993 or 2023will confirm when details load"). Same string verified in the §6.6 side pane at 1280×900 for 1FTZX1762TKB12345. Probe: scratchpad/fa7/states2.spec.ts ("SHEET ambiguous year"), pane.spec.ts. | field |
| R4-J | Fixed at 4e448f1; proved behaviourally, not from the source. With the QR overlay open at 390×844, `canvas.width` = 289 and the CSS box = 289 at devicePixelRatio 1; driving `Emulation.setDeviceMetricsOverride { deviceScaleFactor: 3 }` over CDP on the live page moves devicePixelRatio to 3 and the canvas redraws to backing 867 with the CSS box unchanged at 289 — i.e. `backing === round(css × dpr)` after a ratio change with no resize. QrView.tsx:215-231 now subscribes to `matchMedia("(resolution: <ratio>dppx)")`, guarded for engines without matchMedia. Probe: scratchpad/fa7/stale.spec.ts ("R4-J"). | field |
| A23-a | `bunx vitest run src/lib/storage/db.test.ts --pool=threads` → 7 passed; `--pool=forks` → 7 passed. The row's repro says "→ 5 failed" under threads. Commit 49d1315 replaced the runtime `process.env.TZ` assignment with a stubbed `Date` built from `Intl.DateTimeFormat`, so the zone no longer arrives through the process. (The stryker exclusion it forced is still in place — filed as TA6.) | tests |
| M1 | The mutant the row names as the sharpest, `TextDecoder("utf-8", { fatal: true })` → `{ fatal: false }`, applied to a scratch copy: `bunx vitest run src/lib/payload/` → 6 failed \| 149 passed, where baseline is 155 passed. codec.ts is also at 100% statements and 100% branches in `bun run test:coverage`, against the row's 76.83% mutation score. Commit 679db42. | tests |
| M2 | Both mutants the row names, applied to a scratch copy of extractVin.ts: `valid.length === 1 && wholeRun.includes(...)` → `true && ...` → `bunx vitest run src/lib/vin/` 6 failed \| 347 passed; `run.slice(offset, offset + VIN_LENGTH)` → `run` → 5 failed \| 348 passed. Baseline for that directory is 353 passed. The second-run payload the row asked for exists now. Commit 679db42. | tests |
| M5 | All three mutants the row names, one at a time in a scratch copy, `bunx vitest run src/lib/storage/` (baseline 164 passed): `instant(existing.firstScannedAt) <` → `<=` → 5 failed; `instant(existing.lastScannedAt) >` → `>=` → 5 failed; `metaChanged … \|\|` → `&&` → 6 failed. upsert.test.ts now carries "takes the incoming stamp on a tie, in the min and in the max alike" and the two D11 one-field cases. Commit 49d1315. | tests |
| M6 | All three guards the row names, one at a time in a scratch copy: normalize.ts:24 `if (typeof row.vin !== "string" \|\| !isVinGrammarValid(row.vin))` → `if (false)` → 6 failed; outbox.ts `return Number.isNaN(next) \|\| next <= at` → `return false \|\| next <= at` → 5 failed; db.ts `if (typeof webCrypto?.randomUUID === "function")` → `if (false)` → 6 failed (§5.7's crypto fallback is now taken). Commit 49d1315. | tests |
